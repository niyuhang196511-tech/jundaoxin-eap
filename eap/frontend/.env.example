# 构建期注入（NEXT_PUBLIC_* 会打进 bundle；运行时可在控制台顶栏覆盖凭证）
# 后端 API 基址（默认同源，独立部署时填后端地址）
NEXT_PUBLIC_API_BASE_URL=http://192.168.0.7:8300
# 默认 Bearer Token（建议经 .env.local 覆盖，勿提交真实密钥）
NEXT_PUBLIC_EAP_TOKEN=dev-key-1
