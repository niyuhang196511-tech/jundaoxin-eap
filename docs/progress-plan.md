# EAP 完成计划列表（Progress Plan）

> **用途**：本文件是平台功能的**持久化进度账本**——每项功能带状态、完成描述、剩余工作、验收标准（DoD）、依赖与并行分组。后续开发**照单取任务，完成后回写状态与 commit**，避免每次全库重新探查。
>
> **来源**：基于 [unfinished.md](./unfinished.md)（v1.0 功能全景设计文档）与 v0.7.0 树（b025ecf）实际代码逐项核对生成。
>
> **维护约定**：
> ① 每完成一项 → 把状态改为 ✅、补一行完成描述并附 commit 号；
> ② 新需求追加到对应任务组，不另起文档；
> ③ 每轮开发从「优先级最高且依赖已满足」的任务组取任务，组内各线可**并行推进**；
> ④ 每次收版（chore(release)）同步更新快照头；
> ⑤ 状态图例：✅ 已完成 ｜ 🔶 部分完成（缺项已列）｜ ❌ 未启动。

---

## 快照头

| 项 | 值 |
|---|---|
| 平台版本 | v1.0.0（已发布，tag v1.0.0）+ M36 批次 7（L6 RAG 文档级 ACL，见下） |
| 最新里程碑 | M53（kb-M53A vector-cache 跨库碰撞真修复 / conn-M53B 连接器 DELETE 端点）——M52 遗留登记①③销项（批次 19，两线并发） |
| 代码 commit | 2b49c12 |
| 快照日期 | 2026-09-25 |
| 测试基线 | **SQLite 528 passed + 15 skipped**（fresh，Redis 活体）+ **同库脏复跑两连遍全绿**（M52-D 可重入纪律保持）+ MCP/Redis 集成 6 passed + **PG 实测 541 passed + 2 skipped（fresh）+ PG 同库全量复跑全绿**（第二遍 test_gateway_redis 并发计数用例 1 次时序偶发，单测三连+第三遍全量 541 全绿——观察项入 M53 登记①）+ **ruff F/E9 全树零错** + 控制台 typecheck/vitest 10 条 + **E2E（Playwright）12 条全绿** + openapi 重导出（174 paths / **211** endpoints）+ check_docs/check_observability（7 PASS）绿；CI run 待推送后确认 |
| 下一主线 | M52 遗留①③已销（M53-A/B）；余量见「M53 遗留登记」行；⑦ 仅余 Knowledge 用户主体目录（外部 IdP/SCIM 条件）；外部条件项（SDK 发布/seal/i18n 全量/移动端/alertmanager 真实渠道/L 组）不变；Mimosa git-gate 门禁遗留定性项见 M52 遗留登记⑥ |
| 审计状态 | docs/12（v0.5）/ docs/13（v0.6）/ docs/15（v0.7）/ **docs/16（v0.9.0，方法=逐线审查+继承判定+自动化验证，建议补跑 seal）**；遗留 5 个 MinerU SSRF 维持「补偿控制在位、可接受」判定 |

---

## 一、版本路线总览（unfinished.md §五十）

| 版本 | 主题 | 状态 | 里程碑 | 收版审计 |
|---|---|---|---|---|
| v0.5 | Agent Application（版本层/结构化输出/交互引擎/Action/Artifact） | ✅ 完成 | M18–M22 | docs/12 |
| v0.6 | Platform Governance（工具治理/Policy/评测/成本/Memory 治理） | ✅ 完成 | M23–M26 | docs/13 |
| v0.7 | Extension Platform（统一 Manifest/注册表/Bundle/SDK 契约） | ✅ 完成 | M27–M29 | docs/15 |
| **v0.8** | **Enterprise Integration（Connector/Webhook/Event/IM/A2A/Discovery/Gateway）** | ✅ 完成（七项全部落地：M30 批次 1 + M31 批次 2；真实凭证/外网联调遗留 → L3/L4） | M30–M31 | 待收版审计 docs/16 |
| v0.9 | Production（分布式 Worker/Sandbox/HA/环境体系/CI-CD） | ✅ 完成（P1 Worker/P3 环境体系/P5 CI-CD/P2 沙箱/P4 HA-DR 全部落地） | M32–M33 | docs/16 ✅ |
| **v1.0** | **Enterprise Agent Platform** | ✅ **v1.0.0 已发布**（2026-09-21，tag v1.0.0；14 能力域全 ✅；seal 门禁决策=接受 docs/16 替代，见 docs/16 §三） | M34–M35 + 收版 | docs/16 |

---

## 二、能力域总览（unfinished.md §一 ①~⑭）

| # | 能力域 | 状态 | 完成描述 / 缺口指向 |
|---|---|---|---|
| ① | Identity / Access | ✅ | JWT/OIDC/SSO/API Key 哈希/PostgreSQL RLS/RBAC 收口（`api/security.py`、`runtime/oidc.py`、`observability/rls.py`） |
| ② | Agent Center | ✅ | 配置版本层 draft→publish→archived + rollback + diff + 前端版本抽屉（M18，`runtime/agent_config.py`） |
| ③ | Agent Runtime | ✅ | Loop+HITL 审批+任务恢复+多智能体委派边界+交互挂起+结构化输出（`runtime/loop.py`、`tasks.py`、`multi_agent.py`、`interaction.py`） |
| ④ | Model Center | ✅ | 能力路由+降级链+真流式+定价计量+Mock 确定性合成（`modelhub/`）；vLLM multi-LoRA 悬置 → L1 |
| ⑤ | Knowledge / RAG | ✅ | 三路混合检索+Citation+可插拔组件+RAG 指标评测+MinerU/表格解析；文档级 ACL → L6 |
| ⑥ | Workflow | ✅ | DSL v2 图执行+拖拽画布+interaction 节点+变量快照挂起续跑（`runtime/workflow.py`）+ 版本化/四环境/回滚/画布环境切换（M32，`runtime/workflow_versions.py`） |
| ⑦ | Tool / MCP / Plugin | ✅ | Tool Governance（M24）+MCP Client/Server+插件热载+Extension Platform（M27–29） |
| ⑧ | Connector | ✅ | 端点工具化+风险映射+secret 加密 + SQL 只读连接器/OAuth2 凭证托管/Health Check/Trigger 联动（M31，`runtime/connectors.py`）；真实 IdP/PostgreSQL 运行时联调 → L3 |
| ⑨ | Memory | ✅ | 四层 scope（session/user/agent/org）+importance 加权召回+TTL+LLM 摘要压缩+租户过滤/保留期/批量遗忘导出（M26+M34，`runtime/memory.py`）；组织记忆联动 KB 沉淀 ✅（M41-A，`POST /api/v1/memory/consolidate`）→ L7 收官 |
| ⑩ | Interaction / UI Schema | ✅ | 9 控件+任务/聊天双通道挂起恢复+动态选项级联+自定义 UI 组件 SDK MVP（M20/M29） |
| ⑪ | Artifact / File | ✅ | 产物表+存储+预览/下载+过期惰性剔除+InvokeResult 引用（M21，`runtime/artifacts.py`） |
| ⑫ | Evaluation | ✅ | LLM 裁判多维评分+异步评测+回归对比+RAG 指标 HitRate/Recall/MRR/NDCG（M25） |
| ⑬ | Governance / Policy | ✅ | 6 种策略 kinds+工具治理三件套+成本计量报表+按凭证限流+审计查询页（M23–M26） |
| ⑭ | Observability | ✅ | 结构化日志+/metrics（Prometheus）+OTel tracing+全端点审计（`observability/`） |

---

## 三、已完成明细（v0.5–v0.7 证据索引）

### v0.5 Agent Application（M18–M22，收版 fb63c5e，168 passed）

| 设计项 | 里程碑 | 完成描述（关键证据） |
|---|---|---|
| ① Agent Registry / Version | M18 · 6a1770b | `agent_versions` 表 draft→publish→archived 流水线 + `published_version` 发布指针；运行时覆盖层经 ContextVar 下发（system_role / temperature / 工具白名单 / max_steps / 知识库白名单）；`/agents/{name}/versions` CRUD + publish/deprecate/rollback/diff API（admin+审计）；前端 Agents 版本抽屉。文件：`runtime/agent_config.py`、`api/v1/agents.py`、`migrations/…b3e7f2a9c4d1`、前端 `components/agents/VersionDrawer.tsx` |
| ② Structured Output | M19 · 255955d | `hub.complete(response_schema)`：格式指令注入 → JSON 提取（围栏/prose 容错）→ jsonschema 校验 → 失败带反馈重试一次 → 回退纯文本；openai_compat 走 `response_format json_schema`；优先级：请求级 output_schema > 配置版本 > 代码默认；`InvokeResult.data`；前端 SchemaRenderer 渲染。文件：`modelhub/router.py`、`schemas.py`、前端 `components/chat/renderers/SchemaRenderer.tsx` |
| ③④ Interaction Engine + AI UI Schema | M20 · 292929e / d5cea4b | 9 种控件（text/textarea/number/select/multiselect/radio/checkbox/confirmation/form）+ `data_source` 动态选项（tool 取值/级联/审计）；**双通道挂起恢复**：任务通道 WAITING_INPUT 快照 + interact 端点（镜像 approve）、聊天通道 interactions 表 + submit 端点恢复协议；`ctx.interact` 两态通用；options 端点实时级联取值；内置 warehouse-agent 演示；前端 UISchemaRenderer（必填校验+级联重取）+ ChatPanel 内联表单提交续流 + Tasks 视图填表入口。文件：`runtime/interaction.py`、`migrations/…c5d8f1a2e6b4`、前端 `components/chat/UISchemaRenderer.tsx` |
| ⑤ User Interaction 工作流节点 | M22 · 863cdd3 | DSL `Step.type=interaction`（内联 ui_schema/input_var，parallel/loop 体内禁用）；WorkflowSuspended 变量快照落库 + pending_node；resume_node/resume_values 续跑 + 聊天通道 skip 模式；`POST /workflows/runs/{id}/submit`；workflow-as-agent 挂起 → `InvokeResult.interaction` 与聊天协议统一；画布节点/表单编辑/waiting_input 徽标。文件：`runtime/workflow.py`、`api/v1/workflows.py`、`migrations/…e9f3b6d2a7c5`、前端 `components/canvas/*` |
| ⑥ Action | M21 · ace275a | `runtime/actions.run_action`：UI 动作经 resolve_tool 统一解析（kb/工作流/连接器/插件）执行；requires_approval 高风险动作转 agent.hitl 既有审批流；action.invoke 审计 + 结果写会话记忆；`/api/v1/actions/invoke`；前端 x-actions 动作按钮。文件：`runtime/actions.py`、`api/v1/actions.py` |
| ⑦ Artifact | M21 · ace275a | artifacts 表 + `EAP_MEDIA_DIR/artifacts` 存储（文件名白名单+规范化越界防线）+ `ctx.artifacts.create` + /artifacts 列表/预览/下载（过期惰性剔除）+ `InvokeResult.artifacts` 引用。文件：`runtime/artifacts.py`、`api/v1/artifacts.py`、`migrations/…d7e2a4b8c1f3` |
| ⑧ Dynamic UI Data Source | M20 | data_source（type=tool）动态选项 + 级联重取 + 取值审计（并入 ③④ 交付） |

### v0.6 Platform Governance（M23–M26，收版 81ec0f2，186 passed）

| 设计项 | 里程碑 | 完成描述（关键证据） |
|---|---|---|
| ① Tool Governance | M24 · c827a2a | 工具 `risk_level`/`timeout_s` 元数据（连接器 requires_approval→risk=high）；策略三件套：tool-allowlist 白名单拒绝（PolicyDenied 回注模型）、tool-risk-approval 风险阈值强制审批、agent-allowlist 多智能体委派边界；每次工具调用审计 tool.call（脱敏）+ `eap_tool_calls_total` 指标 + `wait_for` 超时；task 通道策略上下文补齐（消除旁路）。文件：`runtime/policy.py`、`runtime/loop.py`、`api/v1/policies.py` |
| ② Policy Engine 扩展 | M24/M23 | 6 种策略 kinds + config 校验 + 前端配置模板（M23 修复 UI kind 与后端正则失配的存量 bug） |
| ③ Agent Evaluation | M25 · 56a2a33 | LLM 裁判多维评分（正确性/相关性/格式 1–5 分 + judge_model 配置）；评测改异步 `eval.run` 任务；运行历史+详情；回归对比（逐指标 delta + 劣化>1pt 标记）。文件：`api/v1/evals.py` |
| ④ RAG Evaluation | M25 · 56a2a33 | kind=rag 数据集（query + relevant_chunk_ids）+ HitRate@K/Recall@K/MRR/NDCG 确定性指标纯函数 + rag-runs 端点；Knowledge 召回测试面板「标注相关/不相关一键存为数据集」。文件：`runtime/rag_eval.py` |
| ⑤ Cost / Quota | M26 · 2eee165 | 模型定价 price_in/out（每百万 token，admin 注册配置）+ record_usage 计得 cost + UsageRecord agent 归属列；SSE 流式计量归属修复（原记 tenant 0 致预算不熔断）；`/budgets/{tenant}/report` 按模型/智能体/日聚合报表。文件：`modelhub/`、`api/v1/budgets.py`、`migrations/…a2c4e6f8b1d3` |
| ⑥ Permission-aware RAG | ✅ 完成 | KB/WorkflowRecord 补 tenant_id 列 + JWT 通道过滤真实生效（M23）；文档级 ACL / 角色过滤已落地（L6=M36 ce4f97e）；M51-C 角色收敛后 RLS 策略对 JWT 通道真实评估（EAP_DB_APP_ROLE 启用即生效） |
| ⑦ Memory Governance | M26 · 2eee165 | 写入归属租户 + recall/list/history 租户过滤（JWT 通道）；保留期清理（`EAP_MEMORY_RETENTION_DAYS` + /memory/purge）；按用户批量遗忘/导出（数据权利，admin+审计）。文件：`runtime/memory.py`、`api/v1/memory.py` |
| ⑧ Security 收口 | M23/M26 | RBAC 收口（budgets/releases 全写/plugins-reload 补 require_admin）；审计全端点补全 + 前端审计查询页（actor/target/时间过滤+分页）；IM secret Fernet 加密；限流按凭证 429+Retry-After。文件：`api/v1/audit.py`、前端 `views/AuditView.tsx` |

### v0.7 Extension Platform（M27–M29，收版 b025ecf，198 passed）

| 设计项 | 里程碑 | 完成描述（关键证据） |
|---|---|---|
| 统一 Extension Manifest | M27 · 612743a | 8 种扩展 type + `runtime.min_version` 平台兼容检查 + 旧 EAP_PLUGIN dict 归一化兼容。文件：`runtime/extension_manifest.py` |
| 持久扩展注册表 | M27 · 612743a | ExtensionRecord（registered/enabled/disabled/failed 跨重启保持，exposes/error 同步）；plugins.py 加载/热重载与注册表同步（disabled 不执行 register，重载先清注册副作用）；`GET /extensions/registry` + manifest 端点 + enable/disable（admin+审计）；前端扩展目录 Tab。文件：`plugins.py`、`migrations/…bd3e7f2a4c19` |
| .eapext Bundle 生命周期 | M28 · 656531c | 逐条目安全校验（拒绝绝对路径/`..`、文件名白名单、10MB/100 文件上限、resolve+is_relative_to 锁定）+ 安装/升级/卸载全流程 + API（admin+审计）；scaffold 统一 manifest 化 + pack 子命令。文件：`runtime/bundles.py`、`scaffold.py` |
| Extension SDK 契约 | M29 · 8583519 | `eap/ext` 包：RAG SDK（Chunker/Reranker 基类 + `__call__` 委托 + register 转发）、Workflow Node SDK（register_workflow_node）、Model Provider SDK（register_provider 注入路由链）、Connector SDK（register_connector_kind）、UI SDK MVP（register_ui_component）；docs/14 开发者指南。文件：`ext/sdk.py`、`docs/14-extension-sdk.md` |

---

## 四、未完成明细（照单实施区）

> 每组为一条可独立推进的功能线（分支前缀即组名）。组内工作串行、组间并行。
> 「现状」= 已有底座与证据；「剩余工作」= 完成描述；「DoD」= 验收标准。

### 主线：v0.8 企业集成（优先级 1，M30 起）

#### 任务组 A：`event-` 事件中心（M30，底座，先行开工）✅（M30，04cabca）
- **设计依据**：unfinished.md §二十八 Event/Trigger（Cron/Webhook/Event/Message/File Upload/Business Event）。
- **完成描述（M30，commit 04cabca）**：① 进程内事件总线 `runtime/events.py`（通配订阅、队列满丢弃不阻断、可选 Redis pub/sub 跨副本，离线纯进程内可跑）；② TriggerRule 三源触发 `runtime/triggers.py`（event/cron/webhook → agent/workflow 走既有任务通道，min_interval_s 限流 + trigger.fire 审计）+ `api/v1/triggers.py` CRUD（admin+审计，async def 保证 reload 在事件循环上）；③ 入站 webhook `POST /triggers/webhook/{id}`（HMAC-SHA256 签名 + X-EAP-Event-Id LRU 去重）；④ 事件发射点：agent.run.completed / task.completed|failed / workflow.run.finished / kb.document.indexed；迁移 `a3e5f7b9c1d2`；`tests/test_events_triggers.py` 8 用例离线绿。
- **现状**：定时触发已有（TaskScheduleRecord cron/interval，`runtime/tasks.py`，ops-M17.1/2）；任务队列/恢复/审计链完整；Webhook 仅有 IM 平台入站回调（`runtime/im.py`）；**无统一事件总线、无业务/文件事件、无入站 Webhook 触发器**。
- **剩余工作**：
  1. 进程内事件总线（发布/订阅；可选 Redis pub/sub 跨副本）+ 核心事件目录（`agent.run.completed`、`task.*`、`workflow.run.finished`、`kb.document.indexed`、`connector.*`）；
  2. TriggerRule 模型 + API：事件 / cron / 入站 webhook → 目标 agent/workflow（走既有任务通道，事件 payload 透传为输入）；
  3. 入站 webhook 触发端点（签名校验 + 去重）；
  4. 触发审计 + 触发限流（防风暴）；测试覆盖。
- **DoD**：配置「业务事件 → Agent 触发 → 产出 Artifact → 落审计」全链路测试绿；入站 webhook 带 HMAC 校验触发 agent；重复投递幂等。
- **依赖**：无（B、C-Trigger 的底座）。

#### 任务组 B：`webhook-` 对外 Webhook 推送（M31）✅（M31，8289327）
- **设计依据**：unfinished.md §三十五（接入方式：Webhook 出站）。
- **现状**：无出站 webhook；事件总线未建。
- **完成描述（M31，8289327）**：① 端点模型 `WebhookEndpointRecord`（URL / secret Fernet 加密 / events JSON 订阅 pattern 列表，fnmatch 通配）+ 投递记录 `WebhookDeliveryRecord`（payload/attempts/next_retry_at/status pending|done|dead/response_status），迁移 `c6f0a2b4d8e1`（接 b4c6d8e0f2a4）；② 推送引擎 `runtime/webhooks.py`（lifespan 挂载照 triggers.py：总线全量订阅 → 端点 pattern 匹配 → payload 事件五元组原样 + meta → X-EAP-Signature = HMAC-SHA256(raw_body, secret) 与 M30 入站对称，附 Event-Id/Type/Timestamp 头 → HTTP POST 10s 超时；进程内 asyncio 重试循环照 im_outbound：指数退避 base*2^(n-1) 封顶 300s，`EAP_WEBHOOK_*` 可配，超限 dead 死信；HTTP 发送 `send_webhook` 可注入；每次投递审计 webhook.deliver）；③ API `api/v1/webhooks.py`（整路由 admin：端点 CRUD + 审计 webhook.create/update/delete，URL http(s)/pattern/重名校验，secret 不回显；`GET /deliveries` 按 endpoint/status 过滤 + limit/offset 分页；`POST /deliveries/{id}/redeliver` 死信/在途手工重投 attempts 重置 + 审计；`POST /{id}/test` 样例事件走真实投递通道）；④ 控制台 `Integrations.tsx` 增 Webhooks Tab（端点列表/创建编辑弹窗 secret 不回显/投递记录子列表失败标红/死信重投/试投按钮）+ `lib/api.ts` webhooksApi；⑤ `tests/test_webhooks.py` 11 用例离线绿（签名重算、事件→签名推送 done、失败→重试→成功、超限→dead、deliveries 过滤分页、手工重投、CRUD 非 admin 403、不匹配不投递、试投、加密落库）。
- **剩余工作**：① WebhookEndpoint 模型 + 管理 API（URL/secret/事件订阅列表，admin+审计）；② 事件 → HMAC-SHA256 签名推送 → 指数退避重试（复用任务引擎）→ 死信可见；③ 控制台配置页。
- **DoD**：订阅 `agent.run.completed` 后本地接收端收到带签名 payload；断连重试与死信可查询。
- **依赖**：A（事件总线）。

#### 任务组 C：`connector-` 连接器深化（M31）✅（M31，0fe5328）
- **设计依据**：unfinished.md §二十五 Connector Center（Authentication/Credential/Action/Trigger/Webhook/Health）。
- **现状**：ConnectorRecord + 端点工具化（`runtime/connectors.py`）+ requires_approval→risk=high（M24）+ secret Fernet（M26）；**缺 SQL 连接器、OAuth 凭证托管、Health Check、Trigger 联动**。
- **完成描述（M31）**：① SQL 连接器 kind `register_connector_kind("sql", …)`（endpoints `{name, query, params}`，config `{dialect, database}` 存新列 `connectors.config`；只读白名单——剥注释后首词须 SELECT + 写关键字词边界拒绝（INSERT/UPDATE/DELETE/DROP/ALTER/CREATE/ATTACH/DETACH/PRAGMA/VACUUM/GRANT/REVOKE）+ 拒分号多语句；命名占位 `:name`→`?` 绑定防注入；`asyncio.wait_for` + `to_thread` 超时；行数上限 `EAP_CONNECTOR_SQL_MAX_ROWS` 默认 200 超出截断标 truncated；结果 `{columns, rows, truncated}`；psycopg 缺环境运行时报 EAP-7003 清晰错误，未新增依赖）；② OAuth2 凭证托管（ConnectorRecord 补 `oauth_client_id/client_secret_enc/token_url/access_token_enc/refresh_token_enc/expires_at/scopes` 七列，access/refresh Fernet 加密；client_credentials 工具调用前自动获取 + authorization_code `GET …/oauth/authorize`（一次性 state 防 CSRF）+ `POST …/oauth/callback` + `POST …/oauth/token`（admin 手动获取/刷新，不回显 token）；过期自动 refresh_token 刷新；token HTTP 经可注入 `http_client_factory`（默认 httpx）；REST handler 配置 oauth 即带 `Authorization: Bearer`）；③ Health Check `POST /api/v1/connectors/{name}/health`（admin+审计 `connector.health`：rest GET 5s 2xx=ok / sql SELECT 1 / mock-erp 恒 ok），落库 `last_health_at/last_health_ok` 并在列表 `_view` 透出；④ Trigger 联动：`target_type` 校验增 `connector`，`fire()` 增分支——payload 含 `endpoint`/`arguments` 经 `load_connector_tools` 解析同步执行，审计 trigger.fire（detail 标 connector，ref=结果摘要）；连接器调用路径统一 emit `connector.invoked`（try/except 不阻断）。测试 `tests/test_connector_v2.py` 9 用例离线绿（假 transport 禁真实网络）。迁移 `d7a1b3c5e9f2`（接 c6f0a2b4d8e1）。
- **剩余工作**：psycopg/postgreSQL 运行时未实现（sqlite 首要落地，缺环境报清晰错误）；OAuth authorize 端点从 token_url 推导（`…/token`→`…/authorize`），非该约定 IdP 需显式传 `authorize_url`；多副本部署下 oauth state 为进程内语义。
- **DoD**：sqlite demo 连接器建 Connection → 工具调用 → 审计；OAuth 令牌刷新测试绿。✅
- **依赖**：仅 ④ 依赖 A；①②③ 可即刻并行。

#### 任务组 D：`im-` IM 深化（M30，独立）✅（M30，69aa934）
- **设计依据**：unfinished.md §二十五 + docs/10 遗留（卡片消息、应用级 API、事件重试队列）。
- **现状**：飞书/钉钉/企微 webhook 回调 + secret Fernet（`runtime/im.py` + `api/v1/im.py`）；缺卡片消息、应用级发消息 API、回调事件重试队列；真实凭证联调未做。
- **完成描述（M30）**：① 出站卡片 `runtime/im_outbound.py`（平台无关卡片 → 飞书应用级 im/v1/messages / 钉钉机器人 actionCard / 企微 template_card 三适配器，按钮 value/actionURL/key 编码 Action 协议回调引用，HTTP 发送可注入）；② 应用级凭据 `app_id`/`app_secret_enc`（Fernet 加密，M26 同款；`/channels` 创建 + `/channels/{name}/credentials` 端点，响应不回显）；③ 投递重试队列 `IMOutboundLogRecord`（幂等键 channel+event_key 唯一，direction in|out；进程内 asyncio 循环指数退避 `EAP_IM_RETRY_*` 可配，超限 dead；入站回调业务失败自动入队重投）；④ `POST /channels/{id}/send-card`（admin+审计 `im.send_card`，event_key 幂等）+ `tests/test_im_outbound.py` 13 用例离线绿。迁移 `b4c6d8e0f2a4`（接 a3e5f7b9c1d2）。
- **剩余工作**：真实凭证联调（→ L3 遗留）；通讯录/群管理应用 API 未含。
- **DoD**：三平台卡片下发 mock 测试绿；回调重复投递幂等；凭据加密存储。✅
- **依赖**：无。

#### 任务组 E：`a2a-` A2A 深化 + Agent Discovery（M30，独立）✅（M30，bff9797）
- **设计依据**：unfinished.md §三十五 + docs/04 §5（A2A 1.0 Task 生命周期含流式与异步任务）。
- **现状**：`POST /a2a/rpc`（JSON-RPC message/send）+ `/.well-known/agent-card.json` 单 agent 名片（`api/v1/a2a.py` 122 行）；`a2a.py:6` 自述「流式/推送通知暂不支持」；**平台作为 A2A Client 委派外部 Agent 未做**；平台级 discovery 目录未做。
- **剩余工作**：① `message/stream`（SSE）+ `tasks/pushNotificationConfig` 推送通知；② A2A Client：平台 Agent 委派外部 Agent（委派边界并入 agent-allowlist 策略，复用 M24 delegate 机制）；③ 平台级 discovery 端点（列出全部可发现 agent 的目录）；④ 跨租户委派默认拒绝 + 策略放行。→ ①②③④ 已完成（`runtime/a2a_client.py` + `api/v1/a2a.py` + `tests/test_a2a_v2.py`，15 passed；推送回执覆盖 RPC 内同步完成场景，异步任务引擎完成点未挂钩，见模块 docstring）。
- **DoD**：外部 A2A 客户端流式调用平台 agent；平台 agent 经 delegate 调外部 mock A2A server 全链路测试绿。
- **依赖**：无。

#### 任务组 F：`gateway-` API Gateway（M31，独立）✅（M31，fe64dce）
- **设计依据**：unfinished.md §三十四（Rate Limit/Concurrency/Timeout/Circuit Breaker/Idempotency/Request Size）。
- **现状**：按凭证限流已有（`EAP_CHAT_RATE_LIMIT`，429+Retry-After，覆盖 /v1/chat 与 invocations，M26）；中间件仅有 CORS/Trace/MCPAuth（`main.py`）；**缺并发上限、请求体上限、熔断、幂等键**。
- **完成描述（M31）**：① `observability/gateway.py` 三中间件：RequestSizeLimit（Content-Length 超限 413，默认 10MB）、ConcurrencyLimit（按凭证在途上限 429+Retry-After，内置非流式请求超时 504）、Idempotency（/api/v1 写方法 + Idempotency-Key 头 → 同键重放带 X-Idempotent-Replay，进程内存储 + 在途 Future 合并 + TTL 清扫，Redis 可选分支；SSE 路径 /v1/chat/completions 与 /a2a 及 Accept: text/event-stream 前缀排除）；② CircuitBreaker（按模型滑动窗口错误率 → open → 冷却半开单次探测 → 成功 close/失败 re-open，伪时钟可注入）接入 modelhub 路由：open 模型从降级链剔除、成败记录到 breaker，指标 eap_circuit_state（gauge 0/1/2）+ eap_circuit_trips_total；③ 网关指标 eap_gateway_rejected_total{reason} / eap_idempotent_replays_total / eap_gateway_inflight（gauge），metrics.py 新增 gauge_set；④ config.py 追加 EAP_GATEWAY_*（并发默认 0=关、超时默认 0=关、体上限默认 10MB、TTL 300s）；main.py 中间件按 add_middleware LIFO 逆序注册（请求流经 Size→Concurrency→Idempotency）。零 schema 变更（熔断/幂等均进程内存态）。`tests/test_gateway.py` 16 用例离线绿（中间件单测 asyncio.run 驱动 + TestClient 集成 + 伪时钟熔断 + 路由降级链联动）。
- **剩余工作**：全量回归待三线合并后统一跑；Redis 幂等分支仅实现未测（无 Redis 环境）；chunked 无 Content-Length 请求体不拦（生产前置反代收口）。
- **DoD**：并发超限 429、熔断跳闸与半开恢复、幂等重放不重复执行，均有测试覆盖。✅
- **依赖**：无。

### 次线：v0.9 生产化（优先级 2，v0.8 主体后开工）

#### 任务组 P1：`prod-worker-` 分布式 Worker / 队列深化 ✅（M32，d6b03c8）
- **现状**：AsyncioQueueBackend + 可选 Redis Streams（`EAP_REDIS_URL`，`runtime/tasks.py`）+ 多副本调度锁（multireplica 测试）+ PENDING 崩溃恢复。
- **剩余工作**：多 worker 抢占语义强化（可见性超时/租约）、任务幂等键、队列优先级、独立 worker 进程模式（`python -m eap.worker`）。
- **完成描述（M32）**：① 执行租约：TaskRecord 加 `lease_expires_at` 列，worker 取任务时置 now+`EAP_WORKER_LEASE_SECONDS`（默认 300），终态/取消清空；`_recover_leases` 周期扫描（60s + 引擎启动即时）将「RUNNING 且租约过期」重置 PENDING 重跑（抢占式 UPDATE 防多副本重复入队，与 `_recover_pending` 分工：前者管 PENDING 遗留、后者管 RUNNING 泄漏）；引擎 stop 先置停机标记，在途任务回退 PENDING 并清租约（不误判崩溃）。② 任务幂等键：`submit(..., idempotency_key=None)`，同 key 且在途（PENDING/RUNNING/WAITING_*）命中直接返回既有任务；返回值用 str 子类 `TaskSubmitResult`（值即 task_id + `.existing` 属性），既有调用方（kb/evals/triggers）零改动；API 走 body 字段 `idempotency_key`（`Idempotency-Key` 头已被 M31 网关幂等中间件占用，两层互补），响应带 `existing`。③ 队列优先级：TaskRecord 加 `priority`（数值大优先、同级 FIFO）；AsyncioQueueBackend 改 heapq 堆消费 `(-priority, seq, task_id)`；RedisStreamBackend 双流 `eap:tasks:hi`/`eap:tasks`（priority>0 入 hi，lo 复用基础 stream 名保持既有消费端/监控兼容），消费先 hi 短 block 再 lo，XAUTOCLAIM 两流都做且重投保留优先级。④ 独立 Worker：新 `worker.py`（`python -m eap.worker`，不导入 eap.main），init_db → 打版本/配置日志 → TaskEngine（`EAP_WORKER_COUNT` 默认 2）→ SIGINT/SIGTERM 优雅停（drain 停入队等在途 ≤30s → engine.stop）；config.py 加 `worker_count`/`worker_lease_seconds`。迁移 `a9c1e3f5b7d2`（幂等补列 + idempotency_key 索引，接 d7a1b3c5e9f2）。`tests/test_worker.py` 9 用例离线绿（租约恢复/不误恢复/停机回退/drain/幂等引擎级+API 级/优先级出队序/Redis 双流+XAUTOCLAIM 按 skip 机制/入口冒烟断言不导入 eap.main）。
- **剩余工作**：双进程并发消费压力测试与 Redis 分支联调需真实 Redis 环境（本地不可达，Redis 用例自动 skip；test_tasks_redis 存量用例与双流兼容——priority=0 仍走基础 stream）；多副本同 key 并发提交存在非原子窗口（无部分唯一索引，at-least-once 语义可接受）。
- **DoD**：双进程并发消费不重复、不丢失（压力测试）。租约恢复/幂等/优先级/优雅停已有测试覆盖 ✅

#### 任务组 P2：`prod-sandbox-` 工具/代码沙箱 ✅（M33，94dc33b）
- **现状**：无沙箱（扩展=受信代码，进程内执行，docs/04 §8 既有约定）。
- **完成描述（M33，94dc33b）**：① 新模块 `runtime/sandbox.py`：`SandboxRunner` 子进程受限执行引擎（`sys.executable script_path`，单行 JSON stdin 进/stdout 出协议；超时先 terminate 宽限 3s 再 kill——POSIX `start_new_session`+`os.killpg` 进程组树杀、Windows proc.terminate/kill；stdout/stderr 各 256KB 截断带标记；异常/超时一律返回结构化结果 `{ok, exit_code, stdout, stderr, duration_ms, timed_out, limits_applied, truncated, sandbox_dir}` 不上抛；`run_async` 经 `asyncio.to_thread` 线程池承载防阻塞事件循环，模块级默认实例复用）。资源限制：POSIX `preexec_fn` 设 RLIMIT_CPU/RLIMIT_AS；**Windows 降级语义**（resource 为 POSIX 专属）仅强制超时+FS 隔离+env 裁剪，`limits_applied` 如实反映实际生效项。文件系统隔离为**软隔离：约定+环境裁剪，非容器级**（cwd=一次性临时目录沙箱根，相对写入随执行销毁；不 mounts 不 chroot，绝对路径越界写入无技术阻断，诚实标注于 docstring）。环境裁剪：仅透传 PATH/TEMP/TMP/SYSTEMROOT/LANG 白名单（另注入 PYTHONIOENCODING/PYTHONUTF8 两个协议变量强制子进程 UTF-8，防中文 Windows locale 下 JSON 错乱）。② 脚本工具类型：`Tool` 加 `runtime`（inproc|script，默认 inproc 存量零影响）与 `script_path` 字段；`script_tool()` 工厂（handler 经 SandboxRunner 喂 args JSON/回注结果 JSON，失败回注 `{"error": ...}` 与现有工具失败语义一致）+ `run_script_in_sandbox()` 统一执行入口（工厂 handler 与 loop 路由共用，防绕过）；demo 脚本 `examples/sandbox/echo.py`（回显+pwd+环境键清单）。③ 策略联动：新 kind `tool-sandbox`（config `{"mode": "enforce"|"audit", "tools": [...]}`，policies API 白名单+校验），`policy.check_tool_sandbox()` 返回执行决策（script 工具命中清单必须走沙箱防绕过；inproc 工具 enforce→PolicyDenied「被要求沙箱执行但为进程内实现」/audit→放行+violation）；挂进 loop.py 工具执行块（M24 拦截链中、审批门之后），enforce 拒绝与 audit 放行均落 `tool.sandbox.violation` 审计 + `eap_sandbox_violations_total{mode}`；扩展中心工具目录透出 `runtime` 字段。④ 指标：`eap_sandbox_exec_total{result=ok|timeout|error}` + `eap_sandbox_violations_total{mode=enforce|audit}`。零 schema 变更（策略 kinds 用 PolicyRecord 现有表，脚本工具不落新表）。`tests/test_sandbox.py` 11 用例离线绿（echo 往返/超时杀进程/FS 约定隔离与 cwd 断言/env 裁剪/mem 限额 skipif win32/策略 API 校验/决策矩阵/script_tool 全链路/失败回注/loop 沙箱路由防绕过毒丸验证/enforce 拒绝 inproc+violation 审计），test_tool_governance 等存量回归绿。
- **剩余工作**：软隔离语义边界如实记录——绝对路径越界写入无技术阻断（非容器级，需容器/bwrap 才能升级），越界「拒绝」体现为沙箱根一次性销毁 + 策略审计可观测，FS 隔离测试为约定检查（脚本 os.getcwd()==沙箱根）而非权限断言；Windows 无 rlimit（POSIX 限额用例 skipif win32，跨平台仅超时/FS 约定/env 裁剪可跑）；沙箱执行占 asyncio 默认线程池，大量长时限脚本并发需池容量评估；多副本幂等无涉（每次执行独立临时目录、无共享态）；preexec_fn 为 POSIX fork+exec 传统方案，多线程父进程存在文档化限制。
- **DoD**：超时/越界脚本被隔离拒绝且有测试。✅

#### 任务组 P3：`prod-env-` 环境体系 + Workflow 版本化 ✅（M32，22d7a24）
- **现状**：Agent 配置版本层已有（M18）；Prompt 有版本+回滚；**Workflow 无版本、无环境标签**。
- **完成描述（M32）**：① 模型/迁移：新表 `workflow_versions`（`WorkflowVersionRecord`：workflow_id FK、version 整数递增、dsl JSON 快照、env 可空标签 dev|test|staging|prod、state draft|published|archived、note、published_at，唯一约束 (workflow_id, version)）；`WorkflowRecord` 加 `published_version_id` 生产指针列（按 channel_id 惯例用普通 Integer 不加 DB 级 FK——与 workflow_versions 双向引用成环，SQLite 无法 ALTER 加约束）；迁移 `b8d2f4a6c0e3`（幂等建表+加列，接 P1 线 `a9c1e3f5b7d2`）。② 版本服务 `runtime/workflow_versions.py`：save_draft（max+1 递增）/publish（复用 WorkflowSpec 解析做发布期 DSL 校验，同 env 旧 published 自动 archived，env=prod 同步指针，单 env 绑定语义：跨环境发布=迁移、降级清悬空指针）/rollback（该 env 最近 archived 重发布，prod 同步指针）/diff_dsl（按节点/边 id 结构化差异，形态对齐 agent diff）/resolve_dsl 执行解析链（显式 version > env 指定 > prod 指针 > WorkflowRecord.dsl 兜底——无版本记录时行为不变，存量零影响）。③ 桥接 `workflows.py`：get_spec/load_enabled 走解析链；test_run_async 支持 env/version 覆盖（不落版本）；`refresh_registration` 发布/回滚后按解析链重建 spec → 同模块重注册=替换 → 重启（workflow-as-agent 热更新，语义同 agents 版本发布生效）。④ API `api/v1/workflows.py`：GET/POST `/{name}/versions`、GET `/{name}/versions/{id}`（DSL 全文，画布预览数据源）、POST `/{name}/versions/{id}/publish`、POST `/{name}/versions/{id}/rollback`（写操作 admin+审计 `workflow.version.*`，风格对齐 agents versions）、GET `/{name}/versions/{a}/diff/{b}`、test-run body 增 env/version。⑤ 前端：画布左栏「版本」按钮 + `canvas/VersionDrawer.tsx`（版本列表 + 发布到环境 + 回滚 + 存草稿，精简自 agents VersionDrawer，diff 入口后置）；画布顶部环境选择器（草稿/dev/test/staging/prod）——选环境加载该 env 发布版 DSL 只读预览（未发布提示回草稿，预览中禁用保存）；`lib/api.ts` 增 `workflowVersionsApi`。⑥ 测试 `tests/test_workflow_versions.py` 9 例：草稿递增、dev 发布旧版归档/prod 同步指针、坏 DSL 挡发布、rollback 恢复上一版+指针同步、diff、解析链全序（显式 version > env > 指针 > 兜底，两个内容不同版本以标记工具输出断言）、存量无版本工作流行为不变、RBAC member 403、publish/rollback 后 workflow-as-agent 注册 spec 即时变化。零新增依赖。
- **剩余工作**：画布版本 diff 可视化入口后置；与 canary 联动（P2/发布治理）未做；alembic 升级链依赖 P1 线 `a9c1e3f5b7d2` 先合入。
- **DoD**：Workflow 发布/回滚/环境隔离测试绿；画布可切环境。✅

#### 任务组 P4：`prod-ha-` HA / 备份 / 恢复 / DR ✅（M33，a2928f8）
- **现状**：备份 cron 化（M15.5/M13.3）+ 生产 runbook（docs/11）。
- **完成描述（M33，a2928f8）**：① 一键备份/恢复演练脚本 `scripts/drill_restore.py`（纯标准库，零新增依赖）：backup 子命令按 EAP_DB_URL 形态自动选择——SQLite 走 sqlite3 在线备份 API（一致性快照，不中断写入）/ PostgreSQL 调 pg_dump `-Fc` Custom 格式（SQLAlchemy 连接串自动转 libpq 形态）；drill 子命令（核心 DoD）`--backup`/`--latest` 定位备份 → 临时目录还原（SQLite 直接副本 + 只读预检，坏文件在此得到清晰报错；PostgreSQL pg_restore `--clean --if-exists` 到 `--restore-db` 演练库）→ 独立进程 Alembic `upgrade head` 确认可迁移（当前解释器无 alembic 时回退 `uv run --project eap`）→ 冒烟断言（integrity_check + tenants/agents/tasks 关键表可计数 ≥0 + alembic_version 单头）→ 结构化报告（每步 `[步骤 N]` 行/耗时/结论，`--report-json` 机器可读）→ 清理（`--keep` 或失败时保留现场）；退出码全绿 0 / 失败 1，演练全程只读源库。真实跑通：开发库 eap.db backup（1.1MB）→ drill 5/5 步全绿退出码 0（关键表 tenants=1/agents=4/tasks=0，alembic 单头 b8d2f4a6c0e3），负路径（随机字节伪备份）在还原步报「不是有效的 SQLite 数据库」退出码 1。② 双实例 HA 部署样例 `deploy/`：docker-compose.ha.yml（2×无状态 API 实例 EAP_WORKER_COUNT=0 + 独立 worker `python -m eap.worker` + PostgreSQL/Redis healthcheck + depends_on 启动顺序 + YAML 锚点合并共享环境，镜像经 EAP_IMAGE 注入 ghcr 晋升标签）、nginx.conf（upstream least_conn + max_fails 自动摘除、/health 直通、SSE proxy_buffering off + 3600s 长超时、client_max_body_size 对齐网关 10MB 上限）、k8s-ha.yaml（Secret 占位 + Deployment 2 副本 + Service + /health readiness/livenessProbe + 独立 worker Deployment + resources 建议，样例级并注明生产需补 Ingress TLS/HPA/PDB 与外置 PG/Redis）、prometheus-alerts.yml（8 条规则全部基于 /metrics 实际导出指标名：实例宕机 sum(up{job="eap"})<2 与全宕、5xx 占比>5%、平均延迟、任务失败增速、熔断 eap_circuit_state、Agent 错误、网关在途）。③ runbook 增补 §8「恢复演练与 HA 部署」（drill 用法 + 月度 crontab 演练建议 + compose.ha 启动顺序 + 晋升切换要点 + 告警接入）。验证：三份 YAML pyyaml 解析 + 结构断言通过；check_docs.py 全绿（新增 5 链接有效）。
- **剩余工作**：pg_dump/pg_restore/psycopg 分支无环境未实测（按 runbook §3/§4 既有规程书写，仅 --help 与代码审查交付）；k8s 样例为样例级（Ingress TLS/HPA/PDB/真实 Secret 托管待各环境补齐）；main.py lifespan 固定 task_engine.start(workers=2)，EAP_WORKER_COUNT=0 在 API 实例完全生效需 eap/src 一行改动（改读 settings.worker_count，属并行沙箱线文件域，样例按目标状态书写）；p95 延迟告警与任务积压深度告警分别需 metrics 增加 histogram 桶 / 队列深度 gauge 导出后补充（当前延迟为均值代理，积压无对应指标）。
- **DoD**：一键恢复演练脚本在干净环境跑通。✅

#### 任务组 P5：`ci-` CI/CD 强化 ✅（M32，e49cff6）
- **现状**：GitHub Actions 全量回归 + Docker 镜像 job + Playwright E2E（M17.5/M15.6）。
- **完成描述（M32）**：① 发布流水线 `.github/workflows/release.yml`：push `v*` tag → `release-image` job 构建 `eap/Dockerfile` 推送 `ghcr.io/<owner>/<repo>/eap:<semver>`（去 v 前缀）与 `:latest`，并生成 Release notes（softprops/action-gh-release）；`promote` job（workflow_dispatch，inputs: image_tag/environment）pull→tag→push 重打 `:<environment>` 环境标签，prod 绑定 GitHub environment `production`（审批保护可后配），inputs 白名单校验防注入，ghcr 镜像名全小写规范化；两 job 均 `packages: write, contents: write` + 最小化注释（顶层默认 contents: read）。② 文档/契约一致性检查 `scripts/check_docs.py`（纯标准库，仓库根 `uv run --no-project python scripts/check_docs.py`）：README+docs 相对链接与裸 `docs/...` 引用存在性（锚点/外链/代码块忽略，`docs/16` 前向引用豁免）、全部 mermaid 代码块含 diagrams/*.mmd 轻量校验（声明行/边两侧非空/括号平衡，erDiagram 鸦爪记号不误报）、progress-plan 快照头「代码 commit」git rev-parse + cat-file -e 校验；当前全绿（18 文档 / 62 链接 / 32 裸引用 / 33 mermaid 块），无存量坏链接。③ ci.yml 增独立 job `docs-check`（fetch-depth: 0 + uv setup + workflow YAML pyyaml 解析兜底 + 跑脚本），不影响现有 job。④ docs/11 §7「发布与晋升」+ README runbook 行同步。⑤ 顺手修复存量 bug：ci.yml `Lint (ruff: F/E9 only)` 未加引号，自 M13.1（ef26049）起整个 workflow YAML 解析失败（Actions 会拒绝加载），加引号修复。零后端 schema / runtime 改动。
- **剩余工作**：release.yml 真实运行验证需推送后在 GitHub 观察（tag 触发发布 + promote 两次 dispatch，本地仅 YAML 解析与结构复查）；console 镜像（eap/frontend）自动发布未纳入（CI 仅构建校验）；GitHub Environments 的 production 保护规则需在仓库设置中后配。
- **DoD**：打 tag 自动发布镜像并可晋升环境。✅

### 收尾：v1.0 整合验收（优先级 3）

#### 任务组 V：`v1.0-` 全域整合验收 🔶（收版批次 5 完成，v1.0 正式发布待 L 组消化）
- **完成描述（收版批次 5）**：① 能力域总览终核——14 域全部 ✅（对照 unfinished.md §四十九 能力地图）；② 版本提升 v0.9.0（pyproject + `__init__`）+ README 根目录/eap 双侧特性同步（v0.8/v0.9 增量 + eap/README 过期「已实现 vs 待实现」表替换为账本指引）；③ 收版审计结论 docs/16（方法如实声明：逐线审查+继承判定+自动化验证，建议补跑 seal）；④ 文档一致性检查绿（19 文档/67 链接）；⑤ 全量回归 298 passed 复验版本提升。
- **剩余工作**：~~seal 深度扫描补跑~~（2026-09-21 门禁决策：接受 docs/16 替代；补跑属外部 Mimosa 环境动作、仓库内零可执行物，获得环境后再补，非阻塞）+ ~~测试状态泄漏修复~~（✅ M34 498bdfd 已修，docs/16 §三注）+ ~~L 组清零~~（v1.0.0 已于 2026-09-21 发布，L 组按条件持续消化）。**本组无剩余阻塞项**（M51 回写核实）。
- **依赖**：主线 A~F 与 P 组完成（均已 ✅）。

### 长期悬置（需外部条件或产品决策，按条件逐项消化）

| 编号 | 项 | 阻塞原因 / 说明 |
|---|---|---|
| L1 | vLLM multi-LoRA 托管 + 评测门禁接入模型路由 | 🔶 工程部分全部落地：评测门禁接入路由 ✅（M42-B，736d1bf，详见批次表 8-B 行）：模型上线/续用须过评测门禁——eval-gate 策略从路由降级链剔除未过评测的模型 + 模型直评落 model 列作门禁输入；vLLM multi-LoRA 托管 ✅（M42-A，hash 留待提交，详见批次表 8-C 行，真实 GPU 部署指南 docs/20）——两项合并后 L1 仅剩真实 GPU 环境验证 |
| L2 | Harness 桌面端（Tauri） | **产品形态已决策（2026-09-21，用户确认）**：订阅+快捷调用壳 + **用户数据私有化**（本地数据不出端）+ **自定义本地技能** + **移动远程操作**（参考 Codex/ZCode/workbuddy：IM 卡片远程审批/远程触发/移动监控，不做原生 App）。设计方案 docs/18 四期分解（M37 壳工程/M38 数据私有化/M39 本地技能+远程审批/M40 远程触发+GA）——**已实施完毕**：M37~M40 四期 + v1.1 主线（M43）+ 更新进度条（M46-B）+ 输入改选择（M49-F）+ 性能度量与崩溃兜底（M51-E），见批次 8/8++/12/15/17 |
| L3 | IM 真实凭证联调（飞书/钉钉/企微生产账号） | 需外部账号；代码侧由任务组 D 交付 |
| L4 | SLO 99.9% 验收 + DR 实战演练 | 需生产环境；依赖 P4 |
| L5 | 技能市场分发站点 + 技能包 scripts/assets 附件 | 🔶 附件包工程部分 ✅（M34，d550af3）：bundle 扩展 scripts/（仅 .py，执行走 M33 沙箱 script_tool）与 assets/（白名单扩展），sha256 清单随签名覆盖、安全校验镜像 M28、落盘/清单/下载/导出往返端点、scaffold 模板示例；**分发站点仍待产品决策** |
| L6 | Permission-aware RAG 文档级 ACL | ✅（M36，ce4f97e，设计 docs/17 已批准）：document_acls 表（deny 优先→allow→默认可见，KB 级默认+文档级覆盖）+ 三路检索单点过滤（Reranker 前，计数不泄露） + 图谱概览同步过滤 + 管理 4 端点（admin+审计）+ 前端访问控制弹窗；chunk 级 ACL/group 主体为设计预留 |
| L7 | Memory 深化：LLM 摘要压缩、agent/组织层 scope、importance 权重、组织记忆联动知识中心 | ✅ 工程部分全部完成（M34，49a5ea4 + 6e0710f 接线 + M41-A 沉淀收尾）：scope 扩为 session\|user\|agent\|org（agent 按 agent 列过滤、org 租户内共享）、importance 0~1 加权召回打分（final = cos + 0.3\*overlap + 0.2\*importance）、ttl_days→expires_at TTL（recall/list 默认过滤过期，purge 一并清理）、`EAP_MEMORY_SUMMARY_COMPRESS` 开关的 LLM 摘要压缩（`runtime/context.py` compress_messages：hub.complete 生成摘要→kind=summary 落库→历史替换，失败回退字符截断）；**已接入生产调用链**——`ctx.run_loop` 新增 session_id 参数（order_agent 先行启用），接线测试覆盖摘要调用/替换/落库与未传对照；迁移 c0d3e5a7f9b4；组织记忆联动 KB 沉淀 ✅（M41-A：`POST /api/v1/memory/consolidate`，见批次表 8-A 行，hash 留待提交）→ **L7 工程部分全部完成** |
| L8 | MCP 深化：OAuth（复用 OIDC 客户端）、长连接复用 | ✅（M35，566fd36）：client_credentials 凭证托管（Fernet 加密缓存 + 30s margin 过期自动重取 + 手动刷新端点，模式同 M31 连接器）+ 认证头三级回退（OAuth bearer → api_key → 无）+ per (url, auth 指纹) 连接池长连接复用（token 刷新自动轮换实例）；validate 端点接入认证；完整 MCP 握手 OAuth 联调属 L3 式外部验证（mock IdP 离线 7 测试绿） |
| L9 | 多租户计费细化（单价账单、配额分层） | 成本报表已有（M26），计费产品化待决策 |
| L10 | 评测深化：人工抽检、在线影子流量、A/B 效果报表回流 | 🔶 A/B 报表回流工程部分 ✅（M34，b403ebd）：变体归因埋点（`runtime/prompts.py` resolve_template 实验命中即写审计 prompt.render[experiment/version_selected/picked/percent_b] + variant_scope contextvar；agents 同步调用完成/失败落 agent.run.completed 审计并携带 prompt_variant，经 trace_id 关联 usage_records）；报表 `GET /api/v1/prompts/experiments/{name}/report`（admin+审计 prompt.ab.report，分 variant 聚合渲染数与分流占比 vs percent_b 偏差、归因调用数/成功率、token/成本、延迟 p50/均值，支持 since_hours 时间窗，attribution 字段诚实标注口径）；结论辅助 `POST .../conclude`（admin+审计 prompt.ab.conclude，winner+reason 禁用实验，promote=true 复用版本流水线发布胜出版本）。**归因口径局限（如实）**：renders 精确；调用指标为近似——仅覆盖同步智能体调用内部渲染了实验 prompt 的调用（SSE 流式不落审计不计入；渲染前失败无变体可归因；一次调用渲染多实验按最近命中归因），无数据维度返回 0/null 不编造。零 schema 变更（指标全走审计日志 JSON 聚合）；迁移链修复：e2f5a7b9c3d6 down 改指 c0d3e5a7f9b4 恢复单头线性。~~剩余：人工抽检、在线影子流量~~（✅ M44-A/B，6c82bd6——L10 三项全部落地，本行收官） |
| L11 | A2A 跨租户委派 | ✅ 已关闭——任务组 E ④ 已覆盖（跨租户委派默认拒绝 + `a2a-delegate-allowlist` 策略放行 + 审计，M30） |

---

## 五、并行批次建议

| 批次 | 并行任务组 | 说明 |
|---|---|---|
| **批次 1 ✅** | A `event-` ＋ D `im-` ＋ E `a2a-` | 已完成（04cabca / 69aa934 / bff9797），全量回归 234 passed |
| **批次 2 ✅** | B `webhook-` ＋ C `connector-` ＋ F `gateway-` | 已完成（8289327 / 0fe5328 / fe64dce），全量回归 270 passed——v0.8 七项收官 |
| **批次 3 ✅** | P1 `prod-worker-` ＋ P3 `prod-env-` ＋ P5 `ci-` | 已完成（d6b03c8 / 22d7a24 / e49cff6），全量回归 288 passed；P5 顺带修复 ci.yml 自 M13.1 起的 YAML 解析错误 |
| **批次 4 ✅** | P2 `prod-sandbox-` ＋ P4 `prod-ha-` | 已完成（94dc33b / a2928f8），全量回归 298 passed——**v0.9 五组全部收官**（main.py worker 数接 EAP_WORKER_COUNT 收尾项一并落地） |
| **批次 5 ✅** | V `v1.0-` 收版整合 | 已完成（v0.9.0 发布 + docs/16 审计 + README 双侧同步 + 回归 298 passed）；v1.0 正式发布剩 seal 补扫与 L 组 |
| **批次 6 ✅** | L 组工程部分：memory（L7）＋ evals A/B 报表（L10）＋ skills 附件包（L5）＋ 测试泄漏/调度抖动修复 | 已完成（49a5ea4 / b403ebd / d550af3 + 498bdfd / 88c14b9），全量回归 329 passed |
| **批次 6.5 ✅** | L7 接线（摘要压缩入 run_loop）＋ console 镜像/K8s 补齐（P5/P4 未尽）＋ L8 MCP OAuth | 已完成（6e0710f / 014c993 / 566fd36），回归 337 passed |
| **收版 ✅** | **v1.0.0 发布**（6186583，tag v1.0.0）+ docs/16 门禁决策（无 seal 环境，docs/16 替代） | **14 能力域收官，v1.0 达成** |
| **批次 7 ✅** | M36 L6 文档级 ACL（ce4f97e）＋ **M37 Harness 壳工程基座**（d68359c/250c4cf） | M36 已批准实施完成；M37：Tauri 2 壳（托盘/Alt+Space 热键/关闭驻留）+ 设备绑定认证 （签发/列表/吊销，4 测试）+ Agent 目录订阅与流式快捷调用 MVP；**tauri build 全链路验证通过**（NSIS 安装包产出；本机 winget 安装 VS Build Tools 后） |
| **批次 8（当前）** | **M38 数据私有化 ✅**（998da0f：本地 SQLite 仓/三档数据模式/隐私清单页）；**M39-A 本地技能运行时 ✅（2af88fc）**（harness/skills.rs：Ed25519 验签安装+附件 sha256 复核+四域授权弹窗+沙箱执行，跨语言验签夹具锚定 skill_pkg；cargo test 13 用例/typecheck/tauri build 三绿）；**M39-B 移动远程审批+触发 ✅（2af88fc 同批）**（平台侧，docs/18 §二.5：HITL 挂起→IM 审批卡片推送 notify_hitl（渠道 extra.notify_hitl 开关+event_key hitl:{task_id} 幂等+失败入既有重试队列）、回调识别保留字 task.approve（飞书 value/钉钉 actionURL GET/企微 EventKey）→ 引擎 approve+审计 harness.remote.approve、/task 前缀远程触发 agent.invoke（payload 补 _acl 快照）；test_im_remote 9 用例+存量 im/tasks 回归 24 passed）；**M40-B 移动监控 UI ✅（9417cc4）**（harness/src/MonitorView.tsx + App.tsx「监控」视图接线：任务列表/审批待办 WAITING_HUMAN 置顶（pending_tool 标注 + payload/result 展开）/状态徽章（PENDING 灰/RUNNING 蓝/COMPLETED 绿/FAILED 红/WAITING_* 黄）+过滤下拉/会话历史本机+平台并列（来源标注「本机/平台」，平台会话展开懒拉 messages 回放）/平台不可达→「平台不可达，显示本地缓存」降级仅本地+任务区平台离线横幅/30s 轮询（可视时）+手动刷新/≤600px 单列响应式；typecheck/vite build/tauri build 三绿）；**M40-A 完成回执+设备 user ✅（34654b9）**（平台侧，docs/18 §二.5「完成后卡片回执结果」+设备-用户配对：任务终态→IM 回执卡片 notify_task_done（✅ 已完成/❌ 失败按状态、正文 task_id/类型/输出摘要≤200 字符/错误，幂等键 done:{task_id}:{state}，失败入既有重试队列；仅 agent.invoke/agent.hitl 挂钩，tasks.py 终态点非阻断懒 import）、api_keys 加 device_user 列（迁移 c1d3e5f7a9b1，幂等 add_column）+ POST /auth/devices body.user（JWT 通道缺省 request.state.user）注册响应/列表透出+审计 detail 记 user；test_m40_receipt 11 用例+存量 im_remote/harness_devices/tasks 回归 30 passed；设备↔IM 身份强绑定（需 IM 通讯录映射）如实记 M40 剩余）→ **M40 全部完成**（回执 ✅ / 设备配对最小实现 ✅（IM 身份强绑定留外部联调）/ 移动监控 ✅）；**M41-B Harness 审计上报 ✅（c079453）**（GA 项部分落地，docs/18 M40 后置项：平台 POST /api/v1/audit/harness-report——任意有效凭证可路由但仅设备 Key（note=harness:* 反查在 audit.py 内实现，deps.py 不动）可写、条目 action 强制 harness. 前缀改写防伪造他类审计、actor=harness:\<device\>:\<device_user 或 ？\>、单批 ≤500 条 422、created_at 兼容 epoch 秒/ISO/缺失兜底当前时刻、上报动作自审计 harness.audit.report 仅记条数不含内容；Harness Rust local_db.rs audit 表加 reported 列（PRAGMA 检查+幂等 ALTER，存量库兼容）+ harness_audit_unreported/mark_reported 两 command（连接层内存库直测，HTTP 上报由前端 fetch 承担零新依赖、未用 reqwest）；App.tsx 隐私清单页「审计上报」区块：开关存 settings.reportAudit 默认关闭+说明文案（企业可选·仅技能执行类·不含对话内容）+「立即上报」（未上报 skill.* 条目 → POST → 标记已上报，失败不动本地标记可重试）+上报状态/未上报条数展示；test_harness_audit_report 7 用例+存量 harness_devices 回归 11 passed、cargo test 17 用例（13 存量+4 新增）、typecheck/tauri build 绿）；GA 项全部落地（审计上报 M41-B ✅ / 远程吊销传播+离线降级 M43-A/B ✅ / 自动更新工程接入 M43-C ✅——真实更新源与密钥属部署侧外部条件，docs/21） | Harness v1.1 主线（docs/18）**全部完成** |
| **批次 8-A ✅（M41-A）** | 组织记忆联动 KB（L7 收尾，docs/18 §五，hash 留待提交） | `POST /api/v1/memory/consolidate`（admin+审计 memory.consolidate）：挑选 scope=org 且 importance ≥ 阈值（默认 0.7，可调）且 meta 未标记 consolidated 的记忆（created_at 升序，上限 limit 50）→ 聚合为带批次号 markdown 文档（「组织记忆沉淀 YYYY-MM-DD HH:MM（批次 N）」）→ 经既有摄入管线（分块/嵌入/图谱）写入 KB（默认 org-memory，不存在自动创建，KBCreate 命名语义）+ 新文档自动 role=\* allow ACL（M36 复用，org 沉淀对全员可见、租户过滤仍生效）+ 本批记忆 meta 标记 consolidated/consolidated_into 幂等防重复入选，无候选返回 {consolidated:0} 不建文档不建 KB；runtime 侧 MemoryService.consolidation_candidates helper；test_memory_consolidate 3 用例（沉淀只收高阈值/幂等/ACL+检索可见/无候选不建库/RBAC member 403）+ 存量 memory/kb 回归 30 passed，全部离线确定性 |
| **批次 8-B ✅（M42-B）** | **评测门禁接入路由**（L1 工程部分，docs/10 遗留项，hash 留待提交；与 M42-A vLLM/LoRA 并行） | 新 policy kind `eval-gate`（config {"models": [...], "require_eval": bool, "min_pass_rate": 0.8}）：清单内模型——无 model 评测记录且 require_eval=true、或最新评测 verdict≠PASS、或 pass_rate<min_pass_rate → 从路由降级链剔除（`runtime/policy.py apply_eval_gate`，M31 熔断过滤同位同模式），剔除落审计 `model.eval_gate.blocked`（模型名/原因/策略）不抛错、自然落到下一候选，链全空抛既有 ProviderError 语义；`modelhub/router.py` _complete_chain/stream 在熔断过滤之后接入；**模型直评**（`api/v1/evals.py` + eval.run handler）：run 请求带 model（agent 须留空、仅 agent 数据集，agent/model 二选一 400 校验）→ 用例逐条经 hub prefer=model 调用、verdict/pass_rate/scores 落库复用既有流程（execute_evaluation 重构出共用 _finish_evaluation，发布门禁复用不变）；eval_runs 加 model 列（迁移 d1b3f5a7c9e1 接并行线 c9f1e3a5d7b9 之后，幂等 add_column+索引）；`GET /api/v1/evals/runs?model=` 按模型过滤评测历史（列表/详情透出 model 字段）；policies API 注册 eval-gate kind + config 校验（models 列表/require_eval 布尔/min_pass_rate 0~1 数值）；test_eval_gate 4 用例（纯判定：无策略原链不变/无记录+require_eval 剔除/最新记录 FAIL 与 pass_rate 门槛/清单外不受影响/require_eval=false 放行；路由集成：FAIL 模型剔除落第二个+审计、直评 PASS 后直达、链全空 ProviderError；模型直评落 model 列+按模型过滤+作门禁输入；策略注册校验）+ 存量回归 20 passed（prompts_evals/hub/policies 11 + llm_judge/rag_eval/releases/canary 9），全部离线确定性（mock provider）。**已知局限（如实）**：直评经 hub prefer=model 沿 chat 能力链调用，首选模型供应商失败时会落到链上后续模型应答（后续可改直连 provider 直评）；eval-gate 遵循 Policy Engine 租户上下文语义（任务引擎等平台内部调用不生效，评测任务自身不受门禁拦截） |
| **批次 8-C ✅（M42-A）** | **vLLM Provider + multi-LoRA 托管**（L1 工程部分，docs/10 遗留项，hash 留待提交；与 M42-B 评测门禁并行） | ① VllmProvider（新 `modelhub/vllm.py`，providers.py 仅 get_provider 注册 "vllm"）：继承 OpenAICompatProvider 复用 complete/stream_complete（vLLM 服务 OpenAI 兼容 API），新增 `health(base_url)`（GET {base}/v1/models 列出服务中模型/adapter 名；网络/HTTP 错误返回 healthy=false + error 不抛）与 `load_lora/unload_lora`（POST {base}/v1/load_lora_adapter、unload_lora_adapter，vLLM ≥0.5 且 --enable-lora 启动时支持）；base_url 遵循 OpenAI 兼容惯例含 /v1（如 http://\<wsl-ip\>:8000/v1），管理端点自动剥尾部 /v1 防双拼（含/不含 /v1 两种登记形态均可）；HTTP 经模块级 `client_factory` 构造客户端（transport 可注入，测试 MockTransport 零真实网络），单例经 `get_vllm_provider()` 供路由与 LoRA API 共用。② LoRA 托管注册表：`LoraAdapter` 表（models 末尾追加；迁移 c9f1e3a5d7b9，幂等建表）——name 唯一（=vLLM adapter/模型名）、base_model、source_path（GPU 宿主机 adapter 目录）、served_as（默认=name，注册时落定）、status 状态机 registered\|loaded\|unloaded\|failed、note（失败原因/备注）。③ API `api/v1/lora.py`（prefix /api/v1/lora，main.py 注册）：GET 列表 / POST 注册（served_as 缺省=name）/ DELETE（admin+审计 lora.register/delete；status=loaded 拒删 409 须先 unload）；`POST /{name}/load`、`/{name}/unload`（admin+审计 lora.load/unload；vLLM 地址解析=入参显式 base_url > 模型中心 provider=vllm 且模型名匹配 served 名的记录，均无则 400；成功置 loaded/unloaded，失败置 failed + note 记错误 + 502，可重试往返）；`GET /{name}/health` 透出 vLLM 健康与服务清单 + served 判定；审计与业务同事务提交。④ 部署指南 docs/20-vllm-multi-lora部署指南.md（≤80 行）：WSL2 pip / Docker 两路径、vllm serve --enable-lora --lora-modules 示例、adapter 目录挂载（/mnt/c）、平台侧连接 curl 示例（模型中心注册 provider=vllm → LoRA 注册/加载）、常见坑（WSL2 端口转发/显存/防火墙/load 404）。⑤ tests/test_vllm_lora.py 13 用例离线绿：complete 走 OpenAI 兼容（MockTransport 断言 URL/请求体 model=adapter 名/响应解析）、health 双形态 URL 与不可达 healthy=false、load/unload URL 与体、HTTP 5xx/网络错误 → ProviderError、get_provider("vllm") 注册、CRUD+RBAC（member JWT 403/admin JWT 放行）、load/unload 状态机与失败标记 + 审计落库断言、地址解析三态（显式 > 模型中心 > 400）、hub 路由集成（ModelRecord(provider=vllm, priority=1) 经 /v1/chat/completions 到达 vllm provider，请求模型名=LoRA adapter 服务名，测试收尾禁用模型防污染后续路由链）+ 存量 test_hub/test_metrics 回归 6 passed + create_app 冒烟绿。**迁移链说明（偏离决策，如实）**：任务原定 down_revision='a5c7e9b1d3f5'，但既有 head c1d3e5f7a9b1（M40-A）已挂其下，按字面会成多头使 upgrade head 报错；实际改接 c1d3e5f7a9b1 保持单头线性（a5c7e9b1d3f5 → c1d3e5f7a9b1 → c9f1e3a5d7b9 → M42-B d1b3f5a7c9e1）。**未尽**：真实 GPU 侧部署验证（Windows 本机 vLLM 仅能跑 WSL2/Docker，按 docs/20 手工实施）；vLLM 管理端点自身无鉴权（文档标注仅限内网/本机）。 |
| **批次 8+ ✅（M43）** | **Harness GA 收尾多线并行**：M43-A 远程吊销生效传播 ＋ M43-B 离线降级·本地小模型可选 ＋ M43-C 自动更新（工程部分）＋ M43-D 版本更新推送 | 详见下方四行；验证基线：平台回归 393 + cargo test 22 + tauri build NSIS 绿 |
| **M43-A ✅** | **远程吊销生效传播**（docs/18 M40 后置 GA 项，harness 侧；平台侧吊销端点 M37 已就绪零改动，b2d3445） | App.tsx：`deviceStatus` 三态（未探测/ok/invalid）+ 全部平台调用点统一 `checkAuthStatus(r)` 401 检测（loadAgents/quickCall/registerDevice/pullPackage/pullPubKey/reportAuditNow）+ 60s 周期轻探针（有 Key 期间 GET /agents，401→invalid、2xx→ok、5xx/网络错误不改判定）；invalid 时四视图统一红色横幅「设备凭证已失效（可能被远程吊销）——本地数据不受影响，重新注册恢复」，本地会话/记忆/审计不动、同名重注册可复用（平台 409 语义）；MonitorView：401 与「平台不可达」分流——401 经 `onRevoked` 上抛父级置全局态、红色横幅替代黄色离线横幅，审批待办/任务列表/平台会话三区块按 `platformDown = offline || revoked` 降级渲染且文案区分语义；401 不做本地降级（吊销是治理动作，静默回退本地模型属治理旁路）。顺带修正存量隐患：quickCall 流式结束后读闭包旧 `answer`（恒空串）致云端记忆上行一直是空答案——改为流式累加器 acc（`streamPlatformCall/streamLocalCall` 共用），记忆上行与会话留存均取真实内容。typecheck/vite build 绿 |
| **M43-B ✅** | **离线降级·本地小模型可选**（docs/18 M40 后置 GA 项「离线降级（本地小模型可选项）」，b2d3445 同批） | 设置新增「本地模型地址/本地模型名」两栏（默认 http://localhost:11434/v1=Ollama OpenAI 兼容端点，模型名留空=不启用降级）+「检测」按钮（GET {url}/models 列出可用模型，绿/灰结果提示）；quickCall 重构：拆出 `consumeSSE`（data: 行提取，平台/本地共用）+ `streamPlatformCall`（M37 invocations 协议不变）+ `streamLocalCall`（OpenAI 兼容 chat/completions 流式，delta.content 累加）；降级路由——平台网络错误/5xx/超时且配置了本地模型 → 本地流式应答 + 黄色降级横幅「平台不可达（原因），已由本地模型 X 降级应答——能力受限，对话仅留存本机」；本地模型也未配置/也失败 → 双错误信息如实呈现；401 不降级（见 M43-A）；本地留存（M38 save_session）平台/降级两通道一致生效，云端记忆上行仅平台成功时执行。零 Rust/conf 改动（csp=null，WebView fetch 本地端点无阻拦）；typecheck/vite build 绿 |
| **M43-C ✅** | **自动更新·工程接入**（docs/18 M40 后置 GA 项，Rust 侧 agent 并行线 + 前端接线，d04939d） | **Rust 侧**（新 `src-tauri/src/updater.rs`）：tauri-plugin-updater 2.12 接线——`check_update`/`install_update` 两 command 运行时注入 endpoint/pubkey（`UpdaterBuilder::endpoints()/pubkey()` 覆盖 conf；conf 反序列化强制要求 plugins.updater.pubkey 存在 → tauri.conf.json 放占位公钥 `PLACEHOLDER_PUBKEY`，真实部署 signer generate 后两处替换）；endpoint 三态识别（含 `{{`=完整模板透传 / 尾段 .json=固定清单 / 其余=目录拼 `{{target}}/{{arch}}/{{current_version}}`，`resolve_endpoint` 纯函数）；失败语义契约：空参数唯一 Err 路径，其余一切失败 check → `available=false+error(中文)` 不 panic、install → Err(中文)；check 30s 超时下载不设；Windows 上 `download_and_install()` 启动 NSIS（/S 静默+/R 重启）后插件内自退进程、macOS/Linux 走 `AppHandle::restart()`（tauri core，未引 process 插件）；capabilities 加 `updater:default`；cargo test **22 passed**（存量 17 + 新增 5：UpdateInfo serde camelCase shape/空参数文案/endpoint 三态/错误中文映射/对 127.0.0.1:1 真实异步检查不 panic——`generate_context!`+`.any_thread()` 构建真 App 驱动，`build()` 不进事件循环不建窗口）。**前端接线**：设置新增「更新源地址/更新签名公钥」两栏 + 检查更新按钮（发现新版本显示版本号与 notes）+「下载并安装 vN」按钮（安装后提示自动重启）；类型对齐 UpdateInfo camelCase。**部署指南 docs/21-harness自动更新指南.md**（80 行：signer generate 密钥准备/静态目录托管三件套 latest.json+安装包+.sig/endpoint 三态表与 Nginx try_files 回退/发布流程 createUpdaterArtifacts+TAURI_SIGNING_PRIVATE_KEY/端内 invoke 契约/常见坑 5 条）。**诚实边界**：真实签名密钥与更新源未部署（外部条件）；release 构建强制 https（内网 http 需 conf 显式 dangerousInsecureTransportProtocol）；下载进度条留待后续增强 |
| **M43-D ✅** | **版本更新推送**（M43-C 的推送侧补全：手动检查 → 周期自动检查 + 系统通知提醒，521a263） | **Rust 侧**：tauri-plugin-notification 接入（`.plugin(tauri_plugin_notification::init())`，其 Rust Builder 为私有、注册入口为 init()）；新 command `notify_update(title, body)`——NotificationExt 直发系统 toast（Windows 通知中心），Rust 侧调用不走 JS capability（capabilities 未加 notification 权限）；失败返回 Err 中文说明不 panic（开发态/便携运行 Windows toast 常因 AUMID 缺失失败）。**前端**：周期自动检查 effect（配置更新源+公钥才启用；首查延迟 2 分钟不拖慢启动、此后每 4 小时）——发现新版本 → `updateInfo` 置位（快捷调用页常驻绿色横幅 +「立即安装」一键按钮，手动/自动检查共用）+ 系统通知；**同版本通知节流**（localStorage `harness.lastNotifiedVersion`，升级后自动恢复提醒）；检查静默失败不打扰。**诚实边界**：推送=端内轮询更新清单（静态托管，无长连接）；窗口隐藏到托盘时 WebView 定时器可能被节流（检查顺延、语义不变）；**安装始终需用户确认，不做静默自动安装**；通知失败横幅兜底。验证：cargo check/test **22 passed**（期间修复 target/debug/incremental 缓存损坏导致的 rustc STATUS_STACK_BUFFER_OVERRUN 崩溃，清缓存即恢复）、typecheck/vite build/tauri build NSIS 全链路绿；docs/21 §5.5 补推送语义 |
| 批次 8++ | Harness GA 收尾（M43）＋ L 组其余外部条件项 | vLLM（GPU）/ IM 联调（外部账号）/ SLO（生产环境）/ 计费（产品决策） |
| **批次 9 ✅（M44）** | **评测深化收官 ＋ 观测补全两线并行**：M44-A 在线影子流量 ＋ M44-B 人工抽检（L10 收官，evals 域串行）＋ M44-C 观测指标补全（P4 收官，observability 域 agent 并行线） | 详见下方三行；验证基线：平台回归 404 passed + 8 skipped |
| **M44-A ✅** | **在线影子流量**（L10 评测深化「在线影子流量」，6c82bd6） | `runtime/shadow.py` + shadow_configs/shadow_runs 两表（迁移 e5b7d9f1a3c7 接 d1b3f5a7c9e1，单头线性）；挂钩点=agent 调用完成点（API 同步 + SSE 两通道，交互挂起不触发）——查启用配置 → 抽样率门（random）→ asyncio 后台任务经 registry.invoke 全量执行影子 agent：独立 DB 会话（SessionLocal）+ 独立策略租户上下文，影子 trace = 主 trace + "-shadow"（usage_records 成本独立归属不与主调用混计），任何失败只落 shadow_runs.shadow_error 绝不影响主调用（任务本体永不抛错 + done_callback 兜底日志）；API（/api/v1/evals 域内）：配置 CRUD（admin+审计 shadow.*，source/shadow agent 须存在于 agents 表）、运行列表/详情、对比报表（总量/影子成功率/延迟均值与 p50/exact_match 朴素口径；judge=true 对最近 N 对成功运行做双侧 LLM 裁判评分对比，未配 judge_criteria → 400 不静默降级）。**诚实边界**：影子流量有真实 token 成本（配置人须知）、不经限流与预算熔断（内部调用）、任务通道（agent.invoke 任务）不挂钩仅 API 通道。测试 test_shadow_traffic 5 用例（CRUD+RBAC/rate=1.0 配对落库/rate=0 与停用不触发/失效配置失败落库主调用不受影响/judge 校验）离线绿 |
| **M44-B ✅** | **人工抽检**（L10 评测深化「人工抽检」，6c82bd6 同笔） | review_samples 表（迁移 f7d9b1c3e5a2 接 e5b7d9f1a3c7）；抽样：POST /evals/reviews/sample 从 agent.invoke/agent.hitl 任务快照（input/output 冗余落库，任务清理不影响抽检记录；同源 (source,source_id) 防重复 409；其他任务类型 400）；评审：POST /evals/reviews/{id}/review（admin+审计 eval.review.*，维度限 correctness/relevance/format 各 1-5 可部分评分，越界/未知维度/空 scores 400，已评 409 不可重评，pending→reviewed + reviewed_by/at）；报表：GET /evals/reviews/report?agent=（总量/已评/待评/维度均值/好评率=全部已评维度 ≥4 占比——人工口径与 LLM 裁判分位不同如实标注）。**诚实边界**：评审人=管理员（细粒度审阅角色后置）。测试 test_eval_reviews 4 用例（抽样+防重+评分校验+已评不可重评/专属 agent 报表聚合精确断言/非 agent 任务 400/RBAC member 403）离线绿 |
| **M44-C ✅** | **观测指标补全**（P4 剩余工作：p95 延迟告警与任务积压告警所需的 histogram/gauge，agent 并行线，3b5ef36） | ① `eap_request_latency_seconds` histogram（12 桶 0.01~60s；labels 仅 method+route 路由模板——原始路径高基数、网关拒绝与未命中落 unmatched）：新 observability/latency.py RequestLatencyMiddleware 最外层打点（finally 覆盖 4xx/5xx/异常），替代 TraceMiddleware 均值代理计数器（并入 histogram _sum 消除同名双 TYPE 冲突）；**顺带修复 Windows time.monotonic ~15ms 粒度致快速请求观测值恰为 0 的问题（改用高分辨率 perf_counter，全量回归下的确定性测试污染即此）**；② `eap_task_queue_depth{state}` gauge：/metrics 抓取时按 TaskRecord.state 惰性 GROUP BY 计数（共享 DB 真值；多副本各自刷新会互相覆盖同库视图故不做引擎周期刷新），已知 7 态显式补零；③ 告警 8→10 条：EapLatencyP95High（histogram_quantile>5s）+ EapTaskQueueBacklog（PENDING>100，**max 口径**——双副本各导出同值 sum 会翻倍），原均值规则保留（口径互补）改用 histogram _sum/_count。无新增依赖（metrics 为进程内手写实现非 prometheus_client）。test_metrics 2 新用例（exposition 形态/桶累计单调+Inf==_count；队列深度与 DB 计数一致含补零与清理） |
| **批次 10 ✅（M45）** | **账本小项清零**：M45-A A2A 异步任务推送挂钩（任务组 E 收官，a2a/tasks 域 agent 并行线）＋ M45-B 模型直评直连（M42-B 已知局限修正，evals/modelhub 域） | 详见下方两行；验证基线：平台回归 413 passed |
| **M45-A ✅** | **A2A 异步任务推送挂钩**（任务组 E「推送回执覆盖 RPC 内同步完成场景，异步任务引擎完成点未挂钩」的清账，c5e8a08） | message/send 新增 `params.metadata.async_task=true` 平台扩展分支：registry.get 预校验 → 任务引擎 submit（type=agent.invoke，payload 携带 _tenant_id 既有约定）→ 返回 A2A Task state=working（taskId=引擎 task_id），推送配置按引擎 task_id 预登记 `_A2A_PUSH`；任务引擎终态挂点（TaskEngine._run_one_inner 的 M40-A 回执同位）COMPLETED/FAILED 后 `_notify_a2a` 命中配置即构造 Task 快照经 deliver_push 签名投递（懒 import 防循环依赖、非阻断样式：miss 静默/异常仅告警）；tasks/get 内存 miss 回退引擎映射（8 态→A2A state 映射，payload._tenant_id 租户比对不匹配 -32001 不泄漏存在性，completed artifacts 取 result.output）。**诚实边界**：_A2A_PUSH/_A2A_TASKS 进程内内存态——多副本下执行副本查不到配置即不投递（与多副本 oauth state 同性质）；异步回执失败仅告警不重试（可后挂 IM 重试队列同款机制）。test_a2a_push 7 用例（提交/预拦截/完成回执/失败回执/状态映射/租户隔离/无配置不投递）+ 存量 a2a_v2/m40_receipt/worker 全绿 |
| **M45-B ✅** | **模型直评直连**（M42-B 已知局限「直评经 hub prefer=model 沿链调用，首选失败会落到后续模型应答」的修正，584f00e） | hub.complete/_complete_chain 新增 `only_prefer` 参数：prefer 显式指定时把链钉死为该模型一个元素，供应商失败直抛 ProviderError 不降级——直评测到的必须是目标模型本身（否则门禁输入失真）；execute_model_evaluation 接线 only_prefer=True。直评语义变化（诚实）：目标模型供应商失败 → 逐用例落 error 判 FAIL（此前静默降级可能 PASS/FAIL 双向失真）；熔断/门禁过滤仍作用在钉死后单元素链上；普通调用链不受影响（默认 False）。test_eval_gate 2 新用例（dead-model 直评全用例 error/FAIL 且无降级应答，对照组普通调用同场景降级 mock-llm；直连不存在模型 FAIL 且 error 含模型名） |
| **批次 11 ✅（M46）** | **可操作性补齐三线并行**：M46-A 评测中心三页签（影子流量/人工抽检控制台界面）＋ M46-B Harness 更新下载进度条 ＋ M46-C 政策模板补齐九 kinds（连带修复 a2a-delegate-allowlist 后端登记缺口） | 详见下方三行；验证基线：平台回归 414 + Harness cargo 23/tauri build NSIS + 控制台 typecheck/build |
| **M46-A ✅** | **评测中心三页签**（M44-A 影子流量 / M44-B 人工抽检从仅 API 补成控制台可操作，821b6ae） | Evals 页改 TabBar（Integrations 同款模式）：运行评测（原有能力整体抽为页签）/影子流量/人工抽检。影子流量页签：配置表（生产→影子对/抽样率/裁判标记/启停/删除）+ 创建对话框（含选填评分标准）+ 配对运行表（点行详情：输入/主影输出与错误分栏）+ 对比报表（运行量/失败率/主影延迟均值与 p50/输出一致率；LLM 裁判对比按钮按配置 judge 标记禁用并带对数上限）。人工抽检页签：报表摘要条（总量/已评/待评/三维均值/好评率，agent 过滤联动）+ task_id 抽样入口 + 队列表 + 评审对话框（待评三维 1-5 可部分填写；已评只读回显）。顺带修复 run() 成功 toast 读旧 state 致通过率恒 0% 的存量小缺陷；lib/api.ts 增 shadowApi/reviewApi。零后端改动 |
| **M46-B ✅** | **Harness 更新下载进度条**（M43-C 留待增强收官，171796a） | install_update 的 download_and_install 进度回调落地：on_chunk 累计 downloaded 后 emit `update://progress {downloaded, total|null}`（total=Content-Length，分块传输缺失为 null），下载完成发 `update://installing`；emit 失败静默不影响安装闭环。前端 installUpdateNow 先挂 listen 再 invoke（防小包漏事件）：total 已知渲染百分比进度条，未知诚实降级为已下载 MB 计数不假装百分比；unlisten 双保险。事件名/回调签名对照 tauri-plugin-updater 2.12.0 与 tauri 2.11.6 源码核实。**诚实边界**：进度只覆盖下载阶段，验签/NSIS 安装无回调仅显示文案直至进程重启。新增 payload 形状单测，cargo test 23 绿 |
| **M46-C ✅** | **政策配置模板补齐九 kinds + 后端登记缺口修复**（4b67264） | Governance 政策表单 kind 模板/下拉从 v0.6 三种补齐到九种：+tool-sandbox（M33）/a2a-delegate-allowlist（M30，endpoints/agents 双键——按运行时 check_a2a_delegate 语义预填）/eval-gate（M42-B）；空态文案同步。**连带修复 agent 发现的后端缺口**：a2a-delegate-allowlist 运行时真实生效但 PolicyCreate.kind 正则与 _KINDS 漏登记——控制台创建被 422 拒绝（存量测试绕过 API 直插库未暴露）；补登记 + config 校验（endpoints/agents 至少一项列表）+ API 可创建回归用例（test_a2a_push 8 用例） |
| **批次 12 ✅（E2E）** | **端到端测试 + 浏览器可视化走查**：Playwright 用例 9 条新增（评测中心三页签全链路 / 治理政策九 kinds）+ 浏览器走查 T1~T6（214ecd4） | 走查发现并当场修复：侧栏版本标签 v0.4.0 过期（→v1.0.0）、评审备注缺 placeholder、政策对话框初始 config 空致直接创建 400（改为按 kind 预填）；另确认 eval-gate 策略按设计拦死未过评测模型（测试隔离纪律：用例内必清理）。走查截图留档仓库根 gui-test-screenshots/（M48 批次入库）；E2E 跑法：后端 `uv run python -m eap` + `cd eap/frontend && pnpm exec playwright test` |
| **批次 13 ✅（M47）** | **产品成熟度 P1 六项三线并行**（Explore 盘点 27 条缺口后取 P1）：M47-A RLS 补齐+依赖扫描 ＋ M47-B 审计导出+保留期 ＋ M47-C 部署物资产化+观测栈 | 详见下方三行；验证基线：平台回归 430 passed + pip-audit/pnpm audit 零已知漏洞 + compose.prod 解析 11 服务 + 演练含媒体 6/6 全绿 |
| **M47-A ✅** | **RLS 租户隔离补齐 + CI 依赖扫描**（安全线，36f4a9c） | 逐表核实修正盘点误报（grep 清单「7 张缺 RLS」实为 3 张可启用+4 张豁免）：启用 policies（特例 USING tenant_id=current OR 0，对齐 _policies_for 平台默认回退）/trigger_rules/webhook_endpoints（标准式）；豁免 tasks（租户在 payload JSON 内，列级 RLS 无谓词）、revoked_tokens（全局吊销黑名单，启用反致安全回归）、agent_versions/skills（无 tenant_id 列）。迁移 a7c9e1f3b5d7 往返可逆、SQLite no-op；rls.py 权威清单常量 + 7 条防线测试（防新表再漏/豁免表加列即红）。CI dependency-scan job：pip-audit + pnpm audit --prod --audit-level=high，失败即红；本地实扫两端当前零已知漏洞 |
| **M47-B ✅** | **审计日志导出 + 保留期清理**（审计线，975a472） | 导出 GET /api/v1/audit/export（admin，csv/json，过滤语义与查询端点共用 _filtered_query；StreamingResponse 分块 500 条/独立会话；硬上限 EAP_AUDIT_EXPORT_LIMIT 默认 50000 防拖库不可突破；CSV BOM Excel 直开）；控制台审计页导出按钮所见即所导。保留期 EAP_AUDIT_RETENTION_DAYS（365，0=永久）+ POST /api/v1/audit/purge 显式触发（对齐 memory retention 既有模式——调研确认 memory 本就无自动扫描；自动化可直接调 purge_expired）；动作落审计仅条数。test_audit_governance 9 条；诚实修正：AuditLog 无 tenant_id 列（CSV 列清单未虚构） |
| **M47-C ✅** | **部署物资产化 + 观测栈**（运营线，53cc413） | deploy/docker-compose.prod.yml（11 服务：ghcr 镜像/独立 eap-migrate/strict fail-fast/PG+Redis 不暴露宿主/Milvus profile/backup 带媒体卷）+ .env.example.prod（[*] 必改清单对齐 config.py）；启动 fail-fast：EAP_STRICT_CONFIG=1 时开发默认密钥/缺密钥退出码 78 拒绝启动（insecure_config_problems 与警告共用判定；实包验证 78/直通双向）；drill_restore.py --media-dir（媒体 tar.gz+sha256 清单，drill --media 逐文件复核；实包演练 6/6 全绿，消除库与媒体版本漂移）；deploy/observability/（Loki+Promtail 采 Docker 日志并提炼 level/trace_id 标签 + Grafana 预置数据源与 eap 仪表盘 JSON——指标名取自 M44-C 实际导出 + alertmanager 路由/抑制样例）；runbook §9。核查确认 release.yml release-console job 已存在（控制台镜像发布缺口不成立） |
| **批次 14 ✅（M48）** | **成熟度 P2 九项四线并发**（M48+ 待办任务组第一批）：M48-A 控制台三域界面+成本报表导出 ＋ M48-B 开发者体验四件套 ＋ M48-C 密钥轮换+安全响应头 ＋ M48-D 备份调度三形态 | 详见下方四行；验证基线：平台回归 **455 passed + 2 skipped** + MCP/Redis 集成 6 passed（Redis 63790 真跑）+ 控制台 typecheck/vitest 10 条 + check_docs 绿；顺带修复 test_reencrypt_script 全库断言在共享库下被其他用例密文行污染（全量顺序跑误报）→ 改为只断言本用例行 |
| **M48-A ✅** | **控制台三域界面 + 成本报表导出**（P2 #12/#25 销项，24d9ba3） | 三视图页签化：Integrations 增「触发器」页签（TriggersTab：trigger_rules CRUD，event/cron/webhook 三源）、Models 增「LoRA 适配器」页签（LoraTab：适配器登记/健康检查）、Governance 增「记忆治理」页签（MemoryTab：沉淀/遗忘/导出）。成本报表导出 GET /api/v1/budgets/report/export（admin，CSV/JSON，窗口 1~365 天，与 /{tenant_id}/report 复用 _report_payload 同口径）+ 控制台导出按钮；api.ts 增 triggersApi/loraApi/memoryOpsApi 与导出封装。test_budgets +87 行（导出形状/审计落库/参数校验） |
| **M48-B ✅** | **开发者体验四件套**（P2 #17/#18/#19 销项，c2d749e） | ① 脚手架 5→9 类：+workflow-node/model-provider/connector/ui-component（模板=manifest+入口+README；model-provider 含 complete/stream_complete/health 协议骨架）；② OpenAPI 契约入库：scripts/export_openapi.py 导出 docs/openapi.json（169 paths），CI backend job 增 drift check（重导出 + git diff --exit-code，接口/Schema 漂移未同步即红）；③ 示例库 eap/examples/：rag-pipeline（RAG 组件扩展）+ task-flow（任务引擎调用）；④ docs/14-extension-sdk.md 补九类脚手架与契约说明。test_devx_scaffold 覆盖模板生成与产物可导入 |
| **M48-C ✅** | **密钥轮换 + 安全响应头**（P2 #7/#10 销项，ea6d5e2） | 轮换：新配置 EAP_SECRET_KEY_PREVIOUS（逗号分隔旧密钥列表），security_crypto._fernets 解密按当前→旧密钥逐个回退（新写入恒用当前密钥）；scripts/reencrypt_secrets.py 存量重加密 6 表 10 字段（--dry-run 支持，不可解密行如实列出并 exit 1）；流程=设新密钥→旧值移入 PREVIOUS→跑重加密→移除 PREVIOUS（docs/11 §10）。安全响应头中间件：nosniff / Referrer-Policy / X-Frame-Options DENY / CSP frame-ancestors 'none'；豁免 /sdk/*（第三方 iframe 嵌入通道本体）与 /docs /redoc（Swagger 需外链脚本）。**诚实边界**：500 响应不带本层安全头（外层 ServerErrorMiddleware 生成，既有架构边界）。test_security_rotation 4 用例（回退解密/新密文恒当前密钥/重加密脚本/响应头+豁免） |
| **M48-D ✅** | **备份调度三形态**（P2 #3 销项，0ac110d） | ① k8s：deploy/k8s-cronjob-backup.yaml（每日 02:00 对齐 runbook §3，concurrencyPolicy Forbid，成功留 3/失败留 7；双 initContainer=postgres:16-alpine pg_dump custom 格式 + python:3.12-alpine 媒体 tar.gz+sha256 清单，产物落 PVC）；② systemd：eap-backup.{service,timer}（oneshot，每日 02:00，Persistent=true 停机补跑 + 300s 抖动）+ eap-backup-alert@.service OnFailure 模板单元（journal 记录 + 可选 EAP_BACKUP_ALERT_WEBHOOK 群机器人推送）；③ compose：backup 服务修正为一次性语义 restart:"no"（原 unless-stopped 会在退出后无限紧循环备份）+ 三形态调度交叉引用注释。恢复演练统一复用 scripts/drill_restore.py（含 M47-C --media-dir） |
| **批次 15 ✅（M49）** | **P2 余量四项 + UX「输入改选择」六线并发**：M49-A .env 校验工具 ＋ M49-B 网关 Redis 分布式态 ＋ M49-C PG 实测 ＋ M49-D 观测配置完善包 ＋ M49-E 控制台+画布输入改选择 ＋ M49-F Harness 桌面端选择化 | 详见下方六行；验证基线：SQLite 回归 **478 passed + 10 skipped** + MCP/Redis 6 passed + **PG 实测 486 passed + 2 skipped**（临时容器 5433，隔离 venv）+ 控制台 typecheck/vitest 10 条 + **Playwright e2e 12 全绿** + Harness tsc/vite/cargo 23 绿 + check_docs/check_observability（7+10 PASS）绿 |
| **M49-A ✅** | **.env 校验工具**（P2 #2 销项，0421606） | scripts/validate_env.py：解析 .env（引号/export 前缀/行内注释逐项对照 python-dotenv 语义）→ init-only 源实例化 Settings（_InitOnlySettings 覆写 settings_customise_sources——实测 Settings(**init) 单靠优先级仍吸入宿主 env/CWD .env，必须切源隔离）→ pydantic ValidationError 逐条 → 复用 __main__.insecure_config_problems（与 fail-fast 同一判定，import 无副作用已核实）→ 附加检查：4 密钥非空（补 prod 模板 `KEY=  # [*]` 空串占位漏过 is None 判定的缺口）/SECRET_KEY_PREVIOUS 空项/DB_URL scheme。退出码 0/78（EX_CONFIG 对齐）/2，--json 机器可读；--no-project 裸解释器缺依赖时自动委托 eap/.venv 重跑透传退出码。test_validate_env 10 用例（含宿主 env 污染隔离专测）。**诚实局限**：跨行引号值不支持；类型错误存在时该文件不安全默认值检查跳过（需二次校验） |
| **M49-B ✅** | **网关分布式态 Redis 补齐**（P2 #8 销项，54edf46） | gateway.py 三处进程内存态收敛 Redis（配 EAP_REDIS_URL 启用；未配置/不可达回退进程内行为与现状逐一对齐，每机制降级告警一次）：① 每凭证并发计数——入场 INCR+EXPIRE（MULTI 原子）/finally DECR，TTL=max(4×GATEWAY_TIMEOUT_S,120s) 防崩溃泄漏（fail-open 方向注释写明）；② CircuitBreaker——:open 状态键（TTL=10×cooldown，过期视同 closed）+ total/fail 双 ZSET 滑窗 + 探测单飞 SET NX EX（多副本同时试探=可接受放松语义，文档化）；③ 幂等在途——SETNX 标记（TTL≥60s），后到副本轮询 ≤2s 等已存响应重放，超时 **409+Retry-After**（宁可短暂拒绝不容忍副作用重复执行）。统一 _aioredis_client 0.5s 短超时——顺带修复既有 _redis_get/_redis_put Redis 黑洞时无限挂起隐患。test_gateway_redis 12 用例（回退 4 + Redis 路径 8，双实例共享 Redis 模拟两副本；专用 db6 + 键前缀清理纪律）；test_multireplica/test_tasks_redis 加固跑前 flushdb 隔离。**诚实局限**：降级期间各副本本地状态不回灌；熔断同步 Redis 往返在事件循环内（0.2s 超时封顶，高 QPS+Redis 劣化会放大阻塞） |
| **M49-C ✅** | **PG 实测**（P2 #22 销项，4cb3b0c） | conftest 尊重预设 EAP_TEST_DB_URL（默认 SQLite tmp 行为不变）；CI：backend job 加 redis service（63790 精确对齐 test_tasks_redis db5/test_multireplica db9）+ sync 加 --extra redis（security.py 无条件 import redis.asyncio，此前 CI 靠环境隐性通过），新增 backend-postgres job（postgres:16 service + PG 迁移 dry-run 独立空库 + 主套件 EAP_TEST_DB_URL）；test_rls_behavior 8 条 PG-only 行为测试——NOLOGIN 探针角色 + 事务内 SET LOCAL ROLE 模拟 JWT 租户通道（owner 超级用户天然豁免 RLS 正是 rls.py「API Key 通道=平台身份」语义，测试与生产语义一致）：policies 特例（本租户+tenant_id=0 平台默认互不可见）/trigger_rules/webhook_endpoints 标准式/fail-closed（GUC 未设只见平台行）/豁免表 tasks 全见/平台身份全见；跑完库内零残留。**实测揪出并修复三个真实 bug**：① deps.py `SET LOCAL eap.tenant_id=:t`——PG 的 SET 不支持绑定参数，**JWT 通道所有请求在真实 PG 上 500（M9 以来该路径从未在 PG 跑通）**，改 `SELECT set_config('eap.tenant_id',:t,true)` 等价事务级语义；② webhook 端点删除 FK 违约（deliveries 无 CASCADE，SQLite 不强制 FK 静默通过）→ 显式先删子表；③ 迁移 f0a1b3d5e7c4 裸 PRAGMA 在 PG 断链 → 方言守卫。PG 全量 486 passed（首跑 89 failed 逐类修复）。**登记隐患（未修，M50 候选）**：池化连接设过 tenant_id 后 GUC 残留空串 ''（RESET 同样落 ''），策略谓词 ::int 对 '' 抛错——当前 owner 部署形态不评估策略故不可达，收敛为非 owner 应用角色前必须改 NULLIF(current_setting(...),'')::int（涉 rls.py DDL+迁移+静态防线同步） |
| **M49-D ✅** | **观测配置完善包**（P2 #23/#24 可代码化半区销项，a7ff928） | 补全仓缺失的 deploy/prometheus.yml（scrape eap:8300/metrics——经 external network `eap-backend`（name=${EAP_BACKEND_NETWORK:-eap-prod_default}）接主栈 + rule_files=prometheus-alerts.yml + alerting→alertmanager:9093 + 自监控）；observability compose 加 prometheus v2.54.1（与 AM v0.27 同代；挂主配置/规则/TSDB 卷 30d 保留/--web.enable-lifecycle）；loki-config.yml 替换镜像内置配置——**实测纠正：内置默认 retention_period=0s 永久保留而非旧注释所称 15d**——retention 720h + compactor retention 开启（3.x 不开不真删 chunk）+ 摄入 8/16MB + max_query_series 2000 + 流上限 20k（trace_id 标签基数属暂缓，根治=structured metadata）+ 查询缓存 100MB + 关遥测上报；alertmanager.yml 占位 receiver 保留 + 注释化挂接样例（SMTP/企微原生 wechat_configs/群机器人须经 prometheus-webhook-dingtalk 类中转——消息体格式与加签校验如实注明）；scripts/check_observability.py（结构断言 7 PASS；--docker 深度校验 promtool check config/amtool check-config/loki -verify-config 10 PASS，镜像拉不动打 SKIP 不误报）；runbook §9.5 内增补「告警链路联调」子块（起栈网络前提/targets 与 rules 验证/临时规则与 amtool alert add 两种触发/路由干跑/真实渠道挂接步骤，命令均实测）。**活体验证**：临时 Prometheus+AM 实触 EapAllInstancesDown 与 amtool 注入告警，severity 两分支分别命中 oncall/ops-channel 路由。**诚实局限**：真实通知触达=外部账号条件；单实例部署 EapAllInstancesDown（sum(up)<2 双实例口径）会常燃——处置选项已注明，规则改动登记 M50 候选 |
| **M49-E ✅** | **控制台+画布「输入改选择」**（体验域 UX 专项，2894abb） | 五视图 ★★★ 全量（复用 ui/Select + VersionDrawer ChipPicker 范本，拉取失败一律诚实降级回自由输入）：Integrations——触发器 target_type→target_name 三源联动 Select（agents/workflows/connectors）、event_type datalist（选项=新端点目录，保留 fnmatch 通配自由输入）、webhook events ChipPicker 多选+通配模板（提交仍逗号串，后端契约不变）、Endpoints 结构化行编辑器（name/tool_name/path/method/approval，sql kind 追加 query；JSON 解析失败降级 textarea 不丢数据）；Tasks——task_type 限集 Select（对齐 runtime/tasks.py 注册表，未知类型后端直接 FAILED）+ agent.* 时联动 Select 合并 payload（非法 JSON 不覆盖仅提示）；Evals——运行目标按数据集 kind 联动（agent→agents/rag→kb）、影子双 agent Select、抽检 task_id Select（近 50 条 agent.invoke/hitl）+agent 过滤+三维评分 1-5 Select；Governance——门禁 dataset Select、记忆 KB datalist（保留「不存在自动建」语义）、灰度模型 Select 替换硬编码 mock-llm；Models——caps 多选 chips 暴露后端全部 10 种能力（原 UI 只提示 3 种）、LoRA base_model datalist（vLLM 实际基座可不在登记列表，保留自由输入）。画布 ConfigDrawer 五+一处——LLM 模型（auto+登记列表）/知识库 ChipPicker（契约 string[] 不变）/retrieve kb/tool datalist（含 kb.<库>.search 生成项）/subflow Select（新增 workflowName prop 自引用过滤，WorkflowCanvas 已接线）/循环体子步骤 datalist 顺手；useCatalog 抽屉打开惰性拉取、失败按键重试+告警一次。**两 bug 修复**：连接器 kind state 存在但无 UI 控件静默提交 rest→Select（rest｜mock-erp｜sql）；评测「创建数据集」对话框代码齐备但入口不可达→按钮接入+kind Select+cases 模板随 kind（已编辑内容不覆盖）。后端：runtime/events.py EVENT_CATALOG 六事件（grep 全仓 emit 点核实；webhook.test 为试投合成事件不列入，注释说明）+ GET /api/v1/triggers/event-types（admin）+ test_events_triggers 9 绿 + openapi 重导出（+22 行，幂等）。e2e evals.spec 三处 fill→selectOption + 两处既有竞态加固（等真实数据行），12/12 全绿。**诚实局限**：sql 连接器 config.database 表单仍未覆盖（后端 EAP-7003 有指引，UI 加提示文案）；抽检 task_id 数据源为后端固定近 50 条 |
| **M49-F ✅** | **Harness 桌面端「输入改选择」**（体验域 UX 专项，937a225） | App.tsx 三处：① 技能名 pullName→聚焦拉 {baseUrl}/api/v1/skills（Bearer deviceKey + 8s AbortController + 并发保护，401 走全局吊销横幅；鉴权模式照抄 MonitorView.fetchJson）灌 datalist，未配置/失败降级纯自由输入+灰字如实说明原因；② 本地模型名→probeLocalModel 改造（OpenAI 兼容 /models 优先，失败/空兜底 Ollama 原生 /api/tags）成功灌 datalist+绿字提示，「检测」按钮行为不回归（onClick 包一层防事件对象误入 silent 参数）；③ pythonPath→「浏览…」走 tauri-plugin-dialog open()（新增三件套：@tauri-apps/plugin-dialog 2.7.3 + tauri-plugin-dialog crate + capabilities dialog:default + main.rs 注册；Windows exe 过滤/*nix 不过滤），保留手输回退。stdin JSON 预填如实跳过（核实技能详情仅自由文本 instructions 无参数 schema，猜测不可靠）。验证：tsc 零错误 + vite build 成功 + cargo test 23 绿（含 plugin-dialog 编译）。**诚实局限**：文件对话框真实弹窗行为需 tauri dev/打包后人工验证；datalist 为原生控件 WebView2 下样式不可定制 |
| **批次 16 ✅（M50）** | **M50 候选任务组六项全消化五线并发**：M50-A RLS GUC 空串隐患 ＋ M50-B1 策略结构化子表单+租户/连接器补齐 ＋ M50-B2 审计动作目录+schema 表单三件 ＋ M50-C SDK 打包就绪 ＋ M50-D 告警口径+i18n 接线 | 详见下方五行；验证基线：SQLite **488 passed + 12 skipped** ＋ MCP/Redis 6 passed ＋ **PG 实测 498 passed + 2 skipped**（含新迁移往返）＋ 控制台 typecheck/vitest 10 条 ＋ **Playwright e2e 12 全绿** ＋ SDK 产物五点验证 ＋ check_docs/check_observability（--docker 10 PASS）绿；另接管并行会话回归实测产出的三处测试竞态/隔离加固（cb49050 tasks XACK 时序 / 76fd783 幂等轮询预算 / 95ebcd0 m40 回执按任务 id 过滤） |
| **M50-A ✅** | **RLS 策略谓词 NULLIF 化**（M49-C 登记的 GUC 空串隐患，1159976） | 背景：池化连接一旦设过 eap.tenant_id，事务结束后 GUC 残留空串 ''（RESET/set_config(NULL) 同样落 ''），旧谓词 `current_setting(...)::int` 对 '' 抛 InvalidTextRepresentation——当前 owner 部署形态不评估策略故不可达，但它是 rls.py 设想的「收敛非 owner 应用角色」的拦路石。修复：rls.py 全部 11 处谓词（enable_rls 8 表 + m47a 3 表）统一 `NULLIF(current_setting('eap.tenant_id',true),'')::int`——''/未设置→NULL→行不可见=fail-closed，policies 特例仍见 tenant_id=0 平台默认行，与既有「GUC 未设置」路径语义一致。迁移 e4a8c2f6b9d1（接 a7c9e1f3b5d7 单头线性）：upgrade 复用 rls.py recreate helper——**运行时启用路径与迁移结构性同一套谓词，无法漂移**；PG 限定/SQLite no-op/downgrade 恢复旧谓词（可逆性代价，docstring 注明勿用于新部署）。防线：test_rls_coverage 增运行时⇄迁移 CREATE POLICY 逐条比对；test_rls_behavior 增 '' 残留回归 2 条（NullPool 新连接制造残留后自证 current_setting=''，标准表 fail-closed 不抛错/policies 仍见平台行）。PG 实测：迁移往返 upgrade→downgrade→upgrade（pg_policies 实查 11/11 NULLIF⇄0/11 恢复）+ RLS 18 passed + 全量 498。**诚实局限**：'' 残留回归未做旧谓词下必红的反向实证（守护依据=M49-C psql 实测记录+fixture 自证断言）；角色收敛本身（角色管理/连接身份切换）属部署侧后续 |
| **M50-B1 ✅** | **策略结构化子表单 + 租户下拉 + sql 连接器表单**（M50 候选①⑤消化，8b907a0） | Governance 策略对话框：九种 kind 的 config schema 逐条按后端事实核实（policies.py create 校验 + runtime/policy.py 消费点）后渲染结构化子表单——列表键（models/tools/agents/endpoints）→ChipPicker 多选+自由输入（数据源 /models /extensions/tools /agents；provider 无列表 API→预置 mock/openai_compat/vllm+自定义，未新增端点）、枚举键（threshold low/medium/high、mode enforce/audit）→Select、数值→number、eval-gate→checkbox+float(0~1,step0.05)；缺失可选键回填后端缺省（threshold=high/mode=enforce/require_eval=true，与 runtime or/缺省语义一致）。**四重诚实降级**：未知 kind/JSON 非法/config 含 schema 外未知键（列名提示，防结构化编辑静默丢键）/类型枚举范围不符→JSON 编辑模式+原因展示；双向切换不丢已填数据；行级「复用配置」（后端无策略更新端点，description 如实说明需以新名称创建、同名 409）。后端 GET /api/v1/tenants（admin；RBAC 照 audit.py：路由级 resolve_tenant+端点级 require_admin）+ BudgetsTab 租户 Select（member 可达预算页但列表 403→诚实降级手输+权限提示；当前值不在列表附加 option 保留）。Integrations sql 连接器：dialect Select（仅 sqlite——后端 _sqlite_config 只消费 dialect+database 两键、平台不随附驱动，**未虚构 host/port/user/password**）+database Input+提交前 EAP-7003 校验（database 非空/每端点 query 非空，行编辑与 JSON 双模式都拦）。api.ts 增 tenantsApi/toolsApi；test_tenants 4 用例；e2e governance spec 适配子表单断言（mock-llm chip/require_eval 勾选/0.8 预填）。**诚实局限**：chips 数据源拉取失败静默退化自由输入（对齐 TriggersTab 既有风格）；复用配置提交会为缺失可选键补后端缺省值（语义一致但 JSON 形状多键） |
| **M50-B2 ✅** | **审计动作目录 + schema 表单三件**（M50 候选①消化，bb2fb3c） | ① 目录：扫描全仓 98 个审计留痕调用点→87 静态字面量+2 动态解析值=**AUDIT_ACTIONS 89 项/24 域**（observability/audit_actions.py，对齐 EVENT_CATALOG 先例：目录=文档/提示性质，过滤仍接受任意字符串；harness 客户端上报的 harness.* 前缀运行时动作名如实注明不属目录范围）；GET /api/v1/audit/actions（admin）+AuditView 动作过滤 datalist（取数失败静默降级自由输入；member 403 与页面 admin-only 语义一致）。**双向防漂移防线**：静态字面量⊆目录（漏登记即红）/动态调用点必须白名单登记（防腐化）/目录⊆源码引用（幽灵条目即红）/调用点总数≥90 防空转；唯一动态豁免 runtime/context.py:156（变量转发，解析值 memory.summary_compress* 白名单带注释）。② Extensions 工具试运行：parameters JSON Schema→表单（string→Input/string+enum→Select 含当前值保留/number/boolean→checkbox/标量数组→逗号/enum 数组→chips；required 标星、description 作说明、schema 外多余键原样保留并提示；嵌套 object/anyOf/oneOf/$ref/非字符串 enum/缺 type→整体降级 textarea+原因；已有值反填失败同样降级）。③ 画布 interaction 节点：裸 JSON→结构化字段编辑器（控件类型=UISchemaRenderer UIField 联合八型全量——form 仅顶层、后端拒绝内嵌故不提供；options label/value 行编辑、placeholder、required 开关、上移/下移/删除；id 规则提示对齐后端正则；data_source 等未知键 spread 原样保留+「运行时实时取选项、静态 options 为兜底」提示；非法形状降级 textarea 修正后自动恢复）。④ Knowledge ACL：subject_type=role→datalist（*/admin/member/embed——源自 deps.py 约定，IdP claim 无目录端点如实提示）；user 保持自由输入+说明；顺手五处裸 `<select>`→ui/Select 样式统一。另修 HEAD 既有 audit.py ruff F821（datetime 注解缺模块级 import，零行为变化）。test_audit_actions 防线+目录形状+端点 RBAC 共 5 新用例 |
| **M50-C ✅** | **SDK 打包就绪**（M50 候选③消化；实际发布=外部决策，fa95133） | 硬约束全程守住：零源码移动/复制、in-tree import 零破坏、全量 pytest 绿。Python 侧 sdk/python/：**薄再分发层** eap-sdk——20 个契约符号运行时 re-export 自 eap.ext/eap.agents.sdk/eap.runtime.tools（`eap_sdk.Chunker is eap.ext.Chunker` 同一性冒烟过）；否决 hatch sources 子树映射方案（把平台代码复制进第二发行版→eap/eap-sdk 双 dist 同 import 路径遮蔽+版本漂移，违反硬约束）；依赖声明 dependencies=[]+[platform-git] extra+import 期无平台时友好 ModuleNotFoundError 带完整安装指引（eap 不在 PyPI 时硬依赖会让 pip 解析阶段直接失败连报错都看不到——三方案实测取舍）。版本单一事实源=eap/__init__.__version__：构建期 setup.py 三级推导（平台源码→sdist PKG-INFO 异地重建已实测→env EAP_SDK_VERSION），运行期动态读；hatchling 自定义 version source 存在加载时机限制（只对 builder/metadata-hook/build-hook 自动加载且版本解析先于 build hook，实测后改 setuptools 经典方案，选型过程写入 docstring）。JS 侧 sdk/widget/：@eap/widget 版本自动同步平台，build.mjs **构建期读取平台源**（dist 零入库，git check-ignore 逐条验证）注入版本横幅，--check 漂移守卫供 CI，pnpm pack tarball 恰 3 文件且正文与平台源**逐字节等价**（diff 空）；与服务端 /sdk/eap-widget.js 分发并存（README 载明等价关系）。CI：独立 .github/workflows/sdk.yml（ci.yml 零改动=零冲突面；push(main)/PR/workflow_dispatch+paths 收敛触发；python-sdk job=uv build+wheel METADATA 漂移守卫+临时 venv 装 wheel+平台源码冒烟+sdist 异地重建；widget-sdk job=build+--check+pack+清单/横幅/node --check；publish-pypi/npm **双重门控占位**=workflow_dispatch inputs.publish 默认 false+environment 保护，发布步骤注释态并列明 secrets：PYPI_TOKEN 或 Trusted Publishing（id-token:write 已预留）/NPM_TOKEN）。docs/14 §四打包与分发节（+55 行，check_docs 绿）。**发布前外部动作清单**：PyPI eap-sdk 查名保留/npm @eap scope 定名、tokens+Environments 审批配置、LICENSE 决策（现 LicenseRef-Proprietary 占位）、平台包是否上 PyPI（决定依赖可否升级硬依赖）、契约面 semver 承诺、平台 pyproject version 与 __version__ 双处声明的同源守卫（建议项） |
| **M50-D ✅** | **可用性告警口径修正 + i18n 接线准备**（M50 候选④⑥消化，2810d67 / 7901dd4） | ① 实况与登记有出入：常燃的是 **EapInstanceDown**（`sum(up{job="eap"})<2` 双实例 HA 口径，单机 sum=1 恒触发）而非 AllInstancesDown——修为副本数无关两条：EapInstanceDown `up{job="eap"}==0` 按实例评估（warning，for 2m，annotations 带 {{ $labels.instance }} 定位）；EapAllInstancesDown 保留 `sum(up)==0` 并补 `or absent(up{job="eap"})` 兜底（critical，for 1m——scrape 配置误删致 up 序列整体消失时空集合的 sum 是空向量、==0 不触发的盲区）。promtool check config 实测（check_observability --docker 10 PASS）；runbook §9.5/§8.3 与 prometheus.yml 陈旧口径注释同步（prometheus.yml 非 D 线所有权，主会话补齐；§8.3 属最小必要越界已注明）。**诚实局限**：单实例全宕两条同燃属预期，去重靠 AM 路由/inhibit（未加 inhibit_rules）；promtool 只证语法，firing 行为未起真实实例验证（absent 分支为标准 PromQL 语义）。② i18n 准备：I18nProvider 本身带 'use client'——server layout 直接 import 包裹 children（RSC payload 透传，无需 wrapper 文件）；导出 DEFAULT_LOCALE='zh' 单点收敛默认语言；sidebar 接线前后逐 key 输出一致零回归；**顺带修存量 bug**：sidebar 分组标题直渲 {group.label}——group.label 是词典键（nav.studio 等），分组标题一直显示原始 key，补 t()（React key 改用原始键）；规约落点新建 eap/frontend/README.md（新增文案必须走 t()/存量约 4700 行硬编码中文按批次抽取待产品定语言切换/不擅自引入 i18n 运行时库）。语言切换 UI/locale 持久化=产品决策未做 |
| **批次 17 ✅（M51）** | **CI 清零 + 遗留可代码化项全消化五线并发**：M51-A CI 清零（lint 48 处历史存量+三 job 结构性修复）＋ M51-B UI 一致性（#13 销项）＋ M51-C RLS 角色收敛 ＋ M51-D 会话安全两子项（#11 部分）＋ M51-E Harness 性能度量+崩溃兜底（#27 销项/#26 半区） | 详见下方五行；验证基线：SQLite **502 passed + 15 skipped** ＋ MCP/Redis 6 ＋ **PG 实测 515 passed + 2 skipped**（含迁移 c7e9b2d4f6a8 往返）＋ **ruff F/E9 全树零错（历史首次）** ＋ 控制台 typecheck/vitest 10 ＋ **Playwright e2e 12 全绿** ＋ Harness cargo **38**/tsc/vite 绿 ＋ check_docs/check_observability 绿；**GitHub CI run #32 六 job 全绿（历史首次整体绿）**——backend/backend-postgres/frontend E2E/dependency-scan/docs-check/docker-build（首次真正执行）全过 + SDK workflow 全绿（两轮修复收敛：bf575cb redis 守卫 / 2a8c72e CORS+自影子 spec / 26b0b68 E2E 自管 dev server+预热+阶段注解） |
| **M51-A ✅** | **CI 清零**（CI 自 2026-09-18 有记录以来从未整体绿的历史债务清账，86fede5） | ① lint：backend job 红在 `ruff --select F,E9` 48 处存量（CI 失明期累积）挡住全部后续步骤——全树归零。**两处真 F821**：agents/sdk.py delegated_invoke 引用 registry 单例从未导入（顶层导入会循环——registry.py 惰性反向引用 sdk，作者漏写方法内惰性导入；该缝自诞生调用即 NameError 且零覆盖）→ 方法内导入 + 返回类型收敛（registry.invoke 返回 InvokeResponse 信封而签名承诺 InvokeResult，按契约转换）+ 新增 test_sdk_delegated_invoke 护栏；knowledge/service.py AclContext 注解未导入（`from __future__ import annotations` 惰性掩盖，get_type_hints/mypy 即炸）→ TYPE_CHECKING 导入零行为变化。F401×28 逐一核实非 re-export 后删；F811×4 均 fake_idp 夹具双写模式（两处皆意图本体，行内 noqa）；F841×5 保守清理（保留有副作用调用）。**顺带修出真隔离缺陷**：test_structured_output 发布 faq-agent 7.7.7 output_schema 不清理，泄漏击穿下游 test_prompt_ab（CI 字母序恰好躲过=历史全量绿而组合跑红的原因，纯净克隆复现确证）→ 补收尾用例归零。② dependency-scan：删无 install 的 pnpm 缓存配置（Post setup-node 必红的自身配置 bug）。③ frontend E2E 挂起 1h43m+ 修复：job 补 setup-uv+sync（含 redis extra）；E2E 步骤=playwright install chromium → 后台起后端（sqlite 临时库，lifespan 自动种子）→ /health 90s 轮询（超时打后端日志即红）→ 跑测试 + timeout-minutes:20；**删 E2E_NO_WEB_SERVER 空串**（falsy 歧义源，让 config webServer 起前端 dev——与本地成功跑法一致）；**补 NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8300**（本地靠 git-ignored .env.local 指后端，CI 无此文件则前端打同源 3001 必全败——原 job 即使后端起来也绿不了的另一结构性根因）。④ backend-postgres 红因：纯净克隆 HEAD 与 f1198f0 双树 CI 等价复现（TZ=UTC/全新库/同命令/隔离 venv）均全绿（499/498）——ubuntu runner 特有本地不可复现；job 加 --tb=short+junitxml+failure 工件上传，**待推送后凭工件二次诊断**。⑤ 克隆内 backend 全链路预演：lint→dry-run→全量 490→MCP/Redis 6→openapi drift 零 diff。**推送后 CI 实况（两轮收敛至历史首次全绿）**：一轮（01c13c0）backend/dependency-scan/docs-check 首次绿；backend-postgres 红因实锤=test_multireplica Redis 路径用例无守卫（ubuntu 容器精确复现 506 passed+1 failed：ConnectionError 63790——本地 Windows 复现恒绿系本机 compose redis 常驻掩盖，job 刻意不放 redis service）；frontend E2E 红因一=CORS env 缺失（本地靠 git-ignored eap/.env 放行 3001 跨域，CI 无该文件则浏览器 API 全拦）、红因二=evals.spec 影子侧依赖 dev 库存量 canvas-demo（CI 全新种子库只有 faq-agent）→ bf575cb 补 skipif 守卫 + 2a8c72e 补 EAP_CORS_ORIGINS/改自影子 faq-agent（本地 CI 等价复现：无 CORS 必红、带 CORS 全新库 12/12 绿）；二轮 E2E 仍整步骤撞 20min timeout（annotations 实证 timeout 非断言红）→ 26b0b68 弃 playwright webServer 托管改 CI 自管 dev server（pnpm 对 next 子进程 SIGTERM 转发不可靠的回收挂起风险）+ 路由预热（Next dev 冷编译移出用例时预算）+ 五阶段 ::notice 匿名时间线 + 失败 ::error:: 注解与 e2e-diagnostics 工件 → **run #32 六 job 全绿（docker-build 首次真正执行）** |
| **M51-B ✅** | **UI 一致性统一**（P2 #13 销项——P2 体验域收官，f7d6bcf） | 新增 4 个 ui 组件全站贯通：**ConfirmDialog**（金标准=Tasks 审批确认抽出；6 处 window.confirm 全迁移；5 处**无确认破坏性删除**补确认——Tasks 调度/Integrations webhook/Knowledge 文档（描述如实写明级联清理分块+向量索引+图谱关系不可恢复）/Knowledge ACL/ChatPanel 会话；**全仓 window.confirm 清零**）；**ChipPicker**（5 份逐字拷贝+Extensions 内联收敛为一，emptyHint/disabled props；UISchemaRenderer 版保留并注释原因——{label,value} 分离+逐字段 disabled 无法无损覆盖）；**Checkbox**（accent-brand-500 统一替换 4 处裸 checkbox——Governance/Integrations 两处无 accent 渲染系统默认蓝属真 bug）；**DegradeNote**（amber-600/dark:amber-400 单规则统一 7 处降级提示——含 Extensions 灰/amber 混用纠正、Integrations placeholder 隐藏提示改独立可见）。Table loading prop 贯通 6 视图（ui/table 内置 spinner 死能力激活；消灭「加载中…」借 empty 冒充与首屏空态闪现；Tasks 轮询豁免/Knowledge Skeleton 定为非表格标准）。裸控件清零：最后一处裸 select（Extensions MCP transport）+2 处裸 textarea→Textarea。**真 bug 包**：text-ink-500 无效 token×3（globals.css 无此变量类名空转）、顶栏 TITLES 缺 audit 键（/audit 显示原始 key）、conn 措辞对齐、Models 工具栏无 gap×2、Evals 操作列尺寸/颜色 hack、4 处静默吞错补 toast（Extensions validate 无 catch/RunPanel 重抛/Assets×3/ChatPanel openSession）、死代码与未用 import、两处 JSX 缩进。Governance 灰度比例/导出天数预设 Select（★ 遗留销项）。可选项 5/5 全做（outline variant 删/模式切换 Button 化/评审提交入 footer/WfNode 暗色边框/三条最长空态文案精简）。e2e：evals 影子删除 spec 适配 ConfirmDialog，12/12 绿。**诚实局限**：11 条确认流仅 1 条有 e2e 覆盖；Extensions 内联 chips 换组件后渲染为候选∪已选（行为增强非逐字一致）；Evals 删除钮 danger 实心红与其他视图 ghost 有视觉差（按指示统一 danger，全站 ghost 化如需另立条目） |
| **M51-C ✅** | **RLS 非 owner 应用角色收敛（路线 A）**（M50-A 登记的部署侧后续——策略从「owner 豁免形同虚设」变为 JWT 通道真实生效，2c3e006） | 迁移 c7e9b2d4f6a8（接 e4a8c2f6b9d1 单头线性，PG-only/SQLite no-op/可逆）：幂等建 NOLOGIN 角色 **eap_app**+GRANT——盘点：28 个 JWT 可达 router 请求级覆盖全部 45 表 ORM DML（无 TRUNCATE/DDL/COPY，全仓 grep 核实），故授**应用级 DML 全量**（46 表含 alembic_version+38 序列+schema USAGE），租户隔离交给 RLS 而非表级 GRANT 裁剪（逐表裁剪需端点×通道权限矩阵、漏一张即运行时 42501——取舍入迁移 docstring）；ALTER DEFAULT PRIVILEGES 防后续建表迁移漏 GRANT+尽力 GRANT eap_app TO CURRENT_USER（RDS 非超级用户 SET ROLE 前提，失败仅 NOTICE）。deps.py：JWT 分支 set_config 后、postgresql 守卫内，db_app_role 非空→SET LOCAL ROLE（`^[a-z_][a-z0-9_]{0,62}$` 白名单+仅允许配置值本身——SET ROLE 无绑定参数，字面量内联对齐 rls.py 静态纪律；非法值 fail-closed 抛 EAP-1005 绝不静默回退 owner）。API Key/embed/worker/引擎 reload 平台身份路径零改动=精确实现 rls.py:8-10 设想。测试：HTTP 全链路 PG-only 5 用例（真实 TestClient+fake IdP JWT：policies 跨租户互不可见/triggers 写入隔离/非 RLS 表正常/API Key 对照全见/**租户 B 写 tenant_id=A 被 WITH CHECK 拒绝零落库——收敛红利实证**）+'' 残留×SET LOCAL ROLE 组合+静态防线 3（迁移 DDL/deps 顺序与白名单/注入形态 12 种全拒）。PG 实测：迁移往返（downgrade 后角色/授权/默认权限 0/0/0 全回收）+RLS 24 passed+全量 515。部署物：.env.example.prod 加 EAP_DB_APP_ROLE（空=关闭向后兼容）+runbook §2.1（启用/验证 SQL/回滚=置空重启/RDS DBA 说明）。**诚实局限**：请求内 commit 后 SET LOCAL 失效回退 owner（与 M49-C 登记 GUC 语义一致未扩大改动面）；DEFAULT PRIVILEGES 只覆盖执行迁移角色；启用后写错 tenant_id 以 RLS 违例 500 暴露（防线生效但无优雅 4xx 映射）；**顺带暴露既有测试隔离债务**：脏库复跑 test_connector_v2 等非可重入 127 failed（fresh 库绿，非本批回归——登记 M51 遗留） |
| **M51-D ✅** | **会话安全两子项**（P3 #11 可代码化半区，82b29b4） | a) **session_secret 轮换**：security.py _session_keys() 多密钥链（结构照抄 M48-C _fernets——当前钥在前、session_secret_previous 逗号分隔去空白去重），verify_session 逐把回退（每把仍 compare_digest），签发恒用当前钥；与 secret_key 轮换差异写明：会话令牌短 TTL(2h) 自消亡，四步流程无需重签脚本。b) **嵌入令牌渠道级吊销**：方案一（verify 回查渠道状态）胜出——零迁移（方案二 token_epoch 需动 migrations/）；成本=每请求一次 PK 等值查询（该路径本就有 db.get(Tenant)，嵌入流量低频增量可忽略）；**关键前提修复**：payload 原只有 agent 名（一 agent 可建多渠道，按名回查会退化成 agent 级吊销）→ sign_session 增 channel_id 绑定 payload.cid 实现真·渠道粒度；_channel_active 独立短会话（deps.py 不传请求级会话，先例 security_keys.is_revoked 同款）；渠道删除/查询异常/cid 畸形一律 fail-closed。语义如实：disable 即存量令牌失效、re-enable 恢复（测试固化）；存量无 cid 令牌跳过回查 TTL≤2h 兼容窗口自然消亡（部署不打断在线嵌入会话）。jti 保留原样（注释如实标注预留字段无消费点）。widget 401 核查：无重试风暴（失败仅显示错误气泡，_session 存元素内存刷新即重换），服务端 EAP-1002/1003 错误码明确。SlidingWindow 限流段（M49-B 冻结面）零改动。test_security_session 9 用例+embed.py 存量 lint 清零+openapi 契约同步（两 docstring）。**诚实局限**：吊销可逆（永久杀死需叠密钥轮换或删渠道行）；_channel_active PG 下短暂占第二条池连接；c/d/e/f（per-sub 全端登出/滑动续期/并发上限/cookie 化）挂起不动 |
| **M51-E ✅** | **Harness 性能度量三层+崩溃上报最小准备**（P3 #27 销项/#26 可代码化半区——选型无关、零新外部依赖，beb9646） | 性能：① 采集——前端 perf.ts（performance.now 包装）覆盖 5 条无计时路径：perf.platform_stream（TTFB=首个 SSE 数据块+总时长+bytes）/local_model/skill_pull（bytes 取 Content-Length 优先否则 UTF-8 实算）/agent_list/audit_report；Rust updater.rs 下载 Instant+AtomicU64 计时算速率写 perf.update_download（**单写者设计**：NSIS 安装器拉起后进程即退，前端经事件再 invoke 有竞态丢行——偏差理由入 docstring；installing 事件 payload 从 null 扩展为 {downloaded,elapsedMs,kbPerSec}，旧 null 容错）；全部写本地 audit 表 action=perf.*、detail 紧凑 JSON **三层 512 截断兜底**（前端预截/Rust record_perf/平台 audit.py）；采集失败双静默绝不影响主流程。② 上报——App.tsx 过滤器 skill* 扩为 +perf./crash.（slice 500 对齐平台单批上限；显式同意开关语义不动默认关闭）；平台端零改动承载（harness. 前缀强制后 28 字符<64 上限，动态名合法已对照源码确认）。③ 展示——MonitorView「本机性能」聚合区块（harness_audit_list(prefix="perf.")，LIKE 通配符转义+created_at DESC+limit 钳 1..500；类别×次数×均值×最大值×关键指标，null/0 区分；脏 JSON 静默跳过）。崩溃：**Rust panic hook**（setup 内 set_hook）写 {app_data_dir}/crashes/crash-<UTC毫秒时间戳>-<kind>.log（同名自动 -2/-3；正文 kind/version/time/message/backtrace——Backtrace::capture 尊重 RUST_BACKTRACE；256K 封顶；**panic 路径无 unwrap/无索引/写失败静默——杜绝二次 panic=abort**）+crash.panic 审计行；无 chrono 的 UTC 时间戳（civil 算法）。前端 window.onerror/unhandledrejection→invoke crash_record（3s 同错去重防双路重复）+ErrorBoundary 防白屏（fallback 引导隐私页，componentDidCatch 记 crash.react_render）。命令面：crash_list/read（**文件名白名单校验防路径穿越**）/export（DialogExt 直调，dialog:default 已含 allow-save 经 registry 源码核实——capabilities 零改动）/clear；隐私页崩溃诊断区块（列表/查看/导出/清除并入 clear_local scope=crashes/all）。cargo test **23→38**（crashes 8+local_db 5+updater 2）；tsc 零错误；vite build 成功。**诚实局限**（入码注释+UI 文案）：覆盖面=JS 运行时错误+Rust panic，不覆盖进程 abort/栈溢出/WebView2 渲染进程崩溃/setup 前启动期 panic；无 minidump/符号化——Sentry/breakpad 级全覆盖=#26 选型半区外部事项；计时为端内单调钟非 OTel span（docs/08 规划属后续）；失败路径不记时长 |
| **M48+ 待办任务组** | 成熟度 P2 十三项（按域登记，**M48 销 9 项 + M49 销 4 项，余 2 项挂起（#14/#15 待产品方向）+ #16 剩发布外部动作**）：安全（**#2 .env 校验工具 ✅ M49-A**/**#7 密钥轮换 ✅ M48-C**/**#8 网关分布式态 Redis ✅ M49-B**/**#10 安全响应头 CSP ✅ M48-C**）、运营（**#3 备份 CronJob 化 ✅ M48-D**/**#4 控制台镜像 CI 发布 ✅ 销项——release.yml 已有 release-console**/**#22 PG 实测 ✅ M49-C**）、体验（**#12 控制台三域界面 ✅ M48-A**/#14 i18n 待产品方向/**#13 UI 一致性 ✅ M51-B**/#15 移动端）、开发者（#16 SDK 独立包/**#17 脚手架补 4 类 ✅ M48-B**/**#18 openapi.json+文档站 ✅ M48-B**/**#19 示例库 ✅ M48-B**）、观测（**#24 alertmanager 联调 ✅ M49-D 可代码化半区（活体路由实证；真实通知渠道=外部条件）**/**#25 成本报表导出 ✅ M48-A**/**#23 Loki 生产调优 ✅ M49-D 配置半区（生产日志量验证=外部条件）**）；P3 四项（**#11 会话安全维持 ✅ 可代码化半区 M51-D**——轮换+渠道吊销落地，滑动续期/并发上限/cookie 化挂起/**#27 Harness 性能度量 ✅ M51-E**/**#26 崩溃上报 ✅ 选型无关最小准备 M51-E**——Sentry 级全覆盖待外部选型/#15 移动端待产品方向）；另 UX「输入改选择」专项 ✅ M49-E/F（控制台五视图+画布+Harness） | 外部条件项不变（L1/L3/L4/L5/L9） |
| **M50 候选任务组** | 六项全消化（批次 16）：**① UX ★★ 结构化表单二期 ✅ M50-B1/B2**（策略子表单九 kind/interaction 字段编辑器/工具试运行 schema 表单/审计动作目录 89 项+端点/租户下拉 GET /tenants/Knowledge ACL 主体候选）；**② RLS GUC 空串隐患 ✅ M50-A**（11 谓词 NULLIF 化+迁移 e4a8c2f6b9d1+双向防线；角色收敛本身=部署侧后续）；**③ SDK 打包就绪 ✅ M50-C**（eap-sdk 薄再分发+@eap/widget 构建期取源打包+sdk.yml artifacts；实际发布的外部六动作清单见 M50-C 行）；**④ 告警单实例口径 ✅ M50-D**（常燃者实为 EapInstanceDown，已改副本数无关两条+absent 兜底）；**⑤ sql 连接器 config.database 表单 ✅ M50-B1**（后端事实仅 dialect+database 两键）；**⑥ i18n 低成本准备 ✅ M50-D**（Provider 接线+README 规约+sidebar 原始 key 存量 bug 修复；全量抽取待产品方向） | 余量挂起：i18n 全量抽取/移动端（产品方向）、SDK 发布外部动作、alertmanager 真实通知渠道（外部账号）、L 组外部条件项；P3 已于批次 17 消化（#11 半区/#26 最小准备/#27 全部，见 M51 各行） |
| **M51 遗留登记** | 批次 17 落地过程衍生：① ~~backend-postgres CI 红因待二次诊断~~ **✅ 已定位修复**（bf575cb：ubuntu 容器精确复现 test_multireplica 无 redis 守卫；frontend E2E 亦经两轮修复 2a8c72e/26b0b68）——CI run #32 六 job 全绿收官；② ~~既有测试隔离债务~~ **✅ 已清偿（M52-D：两波 41 测试文件唯一命名+差量清理/teardown，SQLite 同库三连遍全绿 + PG 同库复跑全绿）**；③ RLS 收敛启用后遗留：请求内 commit 后回退 owner 语义（已文档化）、DEFAULT PRIVILEGES 只覆盖执行迁移角色、写错 tenant_id 以 500 暴露无优雅 4xx 映射；④ 会话安全 c/d/e/f（per-sub 全端登出/滑动续期/并发上限/cookie 化）挂起；⑤ 崩溃上报选型（Sentry/breakpad 级）=外部决策，harness 崩溃面板/文件对话框真机行为需 tauri dev 人工验证；⑥ 11 条新确认流仅 evals 影子删除有 e2e 覆盖；⑦ UX 后端依赖四件：~~审计 actor 目录端点~~ **✅ M52-A**、~~抽检任务分页参数~~ **✅ M52-B**、~~连接器 PATCH 编辑流~~ **✅ M52-C**、Knowledge 用户主体目录（IdP/SCIM 外部条件，挂起）；⑧ 既有开放项不变：画布版本 diff 可视化、trace_id structured metadata、双进程并发压测、chunked 请求体前置反代收口、IM 通讯录 API、psycopg 连接器运行时、Environments 保护规则、seal 外部环境、i18n 全量/移动端 | ~~下一批次候选按序：⑦-actor 端点、②测试隔离债务~~ 已于批次 18 消化；后续候选见 M52 遗留登记 |
| **批次 18 ✅（M52）** | **⑦ UX 后端依赖可代码化三件 + ② 测试隔离债务清偿，四线并发**：M52-A 审计 actor 目录端点 ＋ M52-B 抽检任务分页参数 ＋ M52-C 连接器 PATCH 编辑流 ＋ M52-D 全量测试可重入（两波 41 文件 + conftest uname 夹具）＋ 顺带真 bug 修复（SlidingWindow Redis 滑窗成员碰撞） | 详见下方四行；验证基线：SQLite **518 passed + 15 skipped**（fresh 含 Redis 活体）＋ **SQLite 同库脏复跑三连遍全绿（历史首次全量可重入）** ＋ MCP/Redis 6 ＋ **PG 实测 531 passed + 2 skipped（fresh）＋ PG 同库全量复跑全绿** ＋ ruff F/E9 全树零错 ＋ typecheck/vitest 10 ＋ **e2e 12 全绿** ＋ openapi 重导出（174 paths/210 endpoints）＋ check_docs/check_observability 绿；**GitHub CI run #34 六 job 全绿（推送当日 c33946b 一次通过，步骤级核验均真实执行——M51 起「CI 为回归门禁」惯例下首个当日落地当日过门禁批次）**；Knowledge 用户主体目录（⑦ 第四件）留待外部 IdP/SCIM 条件 |
| **M52-A ✅** | **审计 actor 目录端点**（⑦ 第一件销项——账本点名「成本最低」项，93bbba1） | GET /api/v1/audit/actors（require_admin，骨架照抄 /actions）：与 action 静态目录本质不同——actor 是**运行时数据**（jwt:\<user\>/api-key/embed/anonymous/system 半枚举 + harness:\<device\>:\<user\> 动态后缀，无法静态穷举）→ `select(distinct(actor))` 真查询、升序、上限 500、返回扁平数组；目录为提示性质**不构成白名单**（查询端点 ?actor= 精确过滤零改动仍接受任意字符串）。前端 AuditView 操作者过滤（原纯自由文本）挂 datalist——与动作过滤完全同构（失败静默降级自由输入，useId 防 id 冲突）。测试照抄 test_audit_governance actions 先例：uuid 唯一名探针 actor 插行 → 目录包含/distinct 无重复/升序/≤500 + member JWT 403；**顺带清偿该文件脏库非可重入**（导出行数 len==2/5/3 计数断言 → action 名唯一化，过滤/分页断言语义不变）。**诚实局限**：AuditLog.actor 无索引，distinct 为全扫（审计表有 retention purge 机制量级可控，不为此加索引）；超 500 actor 截断无分页（datalist 提示场景不需全量） |
| **M52-B ✅** | **抽检任务分页参数**（⑦ 第二件销项，8398698） | GET /api/v1/tasks 补 limit（默认 50 向后兼容，clamp 1..200）/offset——替换硬编码 `.limit(50)`，对齐 evals list_reviews 既有模式；查询构造顺带整理为 filter→order→limit/offset 自然顺序（原 .limit 先于 .where 拼接，语义等价）。api.ts tasksApi.list(limit?, offset?) 仅显式传参才拼 query；Evals 抽检 task_id Select 数据源改传 limit=200（M49-E 登记的「后端固定近 50 条」局限销项——较老任务进不了抽检下拉）。新 test_tasks_pagination 3 用例：limit/offset 窗口与越界、clamp 边界（0→1/9999→200/-5→0）、默认 50 兼容——**自清理设计**：SessionLocal 直写行带唯一 state 标记（M52B+uuid8），module teardown 按标记删净自造行；该设计经脏库实测证明必要（残留行会污染 test_metrics「逐 state 导出值==DB 计数」断言——跨文件污染在源头掐死） |
| **M52-C ✅** | **连接器 PATCH 编辑流**（⑦ 第三件销项——此前连接器建后不可改只能重建，1aee2c5） | ① GET /api/v1/connectors/{name} 详情端点（require_admin，编辑回填用）：endpoints **全对象数组**/config/header_name/oauth_client_id/token_url/scopes 回显，**secret 一律不回显**——api_key/oauth_client_secret_enc 不出现在响应，以 has_api_key/has_oauth_secret 布尔代之。② PATCH /{name}（require_admin，先例=evals shadow-configs PATCH）：ConnectorUpdate 全字段可选、`model_dump(exclude_unset=True)` 显式提交集语义；**name/kind 不可变**（模型不收该二字段，docstring 写明）；**secret 三态**——字段缺省=保留原值/显式 ""=清除置 None/非空=encrypt_secret 加密覆盖；逐 kind 校验以**合并后最终值**执行（rest base_url 须 http(s)=EAP-7002、sql config.database 必填+每端点 query=EAP-7003，复用 create 语义）；base_url/config/endpoints 任一变更 → **status 重置 "pending"**（连接目标变了须重新 validate，诚实语义入 docstring）；审计 connector.update detail **只记提交字段名排序列表+has_oauth，零 secret 值**；AUDIT_ACTIONS 目录同步补条目（test_audit_actions 双向防漂移强制）。工具池零刷新成本：load_connector_tools 实时读库（runtime/connectors.py:454/468 核实），PATCH 即生效（docstring 写明）。③ 前端 ConnectorsTab 注册对话框改**双模式**：操作列「编辑」→ 详情回填（endpoints 行编辑器直接吃全对象）→ name/kind disabled → 标题/按钮文案切换 → 提交**只发改动字段**（jsonEqual 快照对比；endpoints 基线=行编辑器序列化，防其不保留的 description/params 误判改动）；secret 输入留空=不发（placeholder「已保存（留空保留）」）+ Checkbox「清除已存密钥」勾选发 ""；**顺带补齐注册对话框原缺失的 header_name/api_key/oauth 四字段输入**（如实注：原 UI 与后端契约不齐，编辑流必需故补，create 分支同步提交）；STATUS_TONE 补 pending amber；base_url 变更即时提示「保存后重置 pending」；行编辑丢 description/params 显示 DegradeNote。④ 新 test_connector_patch 11 用例（uuid 唯一名脏库可重入）：基础字段+sql config 落库/status 重置边界（无关字段与同值不重置）/endpoints 替换→GET /tools 实时反映/secret 三态 decrypt 回读+详情无明文/404/member 403/审计 fields 列表且序列化不含 secret 明文/EAP-7002/7003 合并校验且被拒更新不落库。**诚实局限**：encrypt_secret 未配 EAP_SECRET_KEY 时明文兼容形态（既有行为，密文形态归 security_crypto 测试覆盖）；「清除」语义裸 API 调用方发 "" 即触发（前端有 Checkbox 显式入口，API 层无二次确认）；无 DELETE 端点现状未变（重建流仍是改名换名的唯一途径） |
| **M52-D ✅** | **测试隔离债务清偿——全量套件脏库可重入**（② 销项：修复前脏库复跑 127 failed（M51-C PG 实测暴露）→ 修复后 **SQLite 同库三连遍全量全绿 + PG 同库全量复跑全绿**，3cc27aa） | 路线=**唯一命名为主 + 点状差量清理/teardown 为辅**（探查否决库重置夹具：PG 上 drop_all/create_all 会毁掉 RLS 迁移 DDL——POLICY/GRANT 不在 SQLAlchemy metadata；且与 session 级 client + webhook/trigger/task 各引擎内存快照架构冲突，function 级重置必然引入新顺序耦合）。conftest 加 **session 级 uname 夹具**（prefix→uuid4 hex8 后缀，样板=test_webhooks._name；module 夹具场景用文件级 `_SFX` 常量）。**两波共 41 测试文件改造**：一波 26 文件——连接器域最重（test_connector_v2 25 处固定名 + **tool_name 一并唯一化**：resolve_tool 按 enabled 连接器扫描取首个同名工具，残留旧连接器指向已失效 tmp sqlite 不唯一化必炸）；策略域（policies/eval_gate）加 **module 级 autouse PolicyRecord 差量清理夹具**（策略按 kind 检索有语义污染——残留改变「租户自有策略不叠加平台默认」判定，仅改名不够；test_tool_governance 先例推广）；版本域固定版本号 9.9.9/7.7.7/7.7.8→随机段（pattern 校验合规），overlay-dummy/tools-agent 加预清理（删版本行+复位 published_version 指针）；hub dead-model（priority=1 残留污染路由链）收尾停用。二波 15 文件——workflow_versions（10 flow 名唯一化+module teardown 软删防 enabled 残留在后续运行启动时重复注册智能体）、vllm_lora（11 常量）、shadow_traffic（16 名，配置名唯一→报表按配置过滤天然隔离）、scheduler（**根因**：残留启用态 1s demo-cron 调度跨运行持续刷任务污染全库——唯一命名+前清 legacy 固定名残留+后删自造调度+在途落定 sleep 消竞态）、oidc（**根因**：callback 按 sub 复用用户/按 note 复用 ApiKey，残留同 sub 用户→换发返回 api_key None→`.startswith` 炸——sub 唯一化+测后删行）、extension_bundles（manifest 严格 semver 无后缀空间→bundle 名唯一化+测后卸载+cache_clear）、governance_baseline/canary（发布版本号唯一化+canary 取值消歧）、harness 设备域（rep-dev-*/workstation-1 唯一化+用毕吊销）、skill_assets/im（渠道 fs-1 等模块常量唯一化）、kb_acl、worker（UNIQUE tasks.id 固定 id 唯一化+残留 PENDING 行测后删防启动恢复扫描误捞）、events_triggers（规则名+event_type 后缀防残留规则劫持）、a2a 双文件（api_keys.key 唯一约束+策略差量清理**删行**——仅停用不够，唯一约束不看 enabled）、im_remote/m40_receipt（残留 pending 投递入口排涸置 dead+回执卡断言按 task_id 过滤）、memory_consolidate（沉淀 KB 每遍唯一——固定库残留上遍沉淀文档使检索命中旧文档）。主线补两处：test_workflow_interaction（二波清单遗漏）3 flow 名+会话 id 唯一化+module teardown；**test_secret_crypto 残留启用态 priority=90 openai_compat 模型**（base_url 指向 api.example.com）劫持后续所有路由用例先真实出站 ~13s 超时再降级 mock——一波唯一命名反而使其逐遍累积，补 teardown 禁用（对齐 eval_gate/hub 收尾纪律；test_jwt_auth 所建模型 provider=mock 无出站风险核实不改）。**顺带真 bug 修复（src，f408a8a）**：SlidingWindow Redis 滑窗成员为纯 `f"{now}"`——Windows ~15.6ms 时钟刻度下连发请求同刻度撞成员，**ZADD 同成员只覆盖不新增→突发被合并少计、限流失真**（Linux µs 级同刻突发同理可撞）；探针实证 zadd 返回 0/zcard 停滞；修复=成员 `f"{now:.6f}:{uuid4.hex[:8]}"` 唯一化——**gateway 熔断器 M49-B 已有同款纪律（其注释明写「唯一成员（同成员 ZADD 会覆盖计数）」），此为漏网同族**；test_rate_limiter_redis_path 从时运红绿升格为确定性守卫（修复前单跑必红，修复后 5 连跑绿）。语义调整如实登记（未削弱断言）：im_remote/m40 `len(push)==1`→按 task_id 过滤后恰一条（脏库下一遍启动时引擎恢复执行遗留任务，其回执落进同一捕获窗口与被测行为无关——先例=95ebcd0）；`_poll` 缺省 20s→60s（仅容忍降级环境，断言不变）；test_metrics 断言面收敛 TASK_STATES（gauge 按设计只导出已知 state 显式补零，对全库 state 做 == 是误报断言，导出面内仍精确相等）。**DoD 实证**：SQLite 同一持久库全量三连遍 508/518 全绿（Redis 关/开两态各验）+ PG（postgres:16-alpine 临时容器，CI 等价）fresh 531 + 同库全量复跑 531 全绿 + fresh 基线不回退。**诚实局限**：目标=可重入非强隔离——审计表膨胀/已停用记录等语义残留不清理（唯一命名保证断言不再命中）；未引入随机序（无 pytest-randomly），跨文件顺序耦合仅修至「任意连续两遍绿」；PG 脏库第二遍曾现 1 红（test_im_outbound::test_retry_fail_then_succeed）——Docker daemon 中途崩溃致 redis 消失+killed run 半截状态的时性环境扰动（同遍 12 skipped 异常为证），文件级三连跑与全量第三遍均不红，如实记录；**发现未修（登记 M52 遗留）**：knowledge/vector_store.py LocalVectorStore 磁盘缓存 `vector-cache/{kb_id}.npz` **跨库碰撞**——失效键仅 (chunk 数,首尾 chunk_id) 全是每库各自增 id，换库跑测试 id 序列复现而 npz 是旧库内容→加载**别的 KB 的向量**、检索排序张冠李戴（实证：stale 缓存下 rerank/extensions 组合确定性红且失败用例漂移，清缓存后同命令两遍全绿；HEAD 原版同样复现=先于本批存在的存量，CI 恒 fresh 工作区故隐形）——候选修法=cache key 纳入库身份指纹或 npz 内容哈希校验 |
| **M52 遗留登记** | 批次 18 落地过程衍生：① ~~vector-cache 跨库碰撞真 bug~~ **✅ 已修复（M53-A：npz 内容指纹校验，受控毒缓存 A/B 实证）**；② ⑦ 第四件 Knowledge 用户主体目录挂起不变（外部 IdP/SCIM 条件——主体候选端点需真实用户目录数据源）；③ ~~连接器无 DELETE 端点~~ **✅ M53-B（DELETE+触发器引用 409 阻断+前端 ConfirmDialog）**；④ 确认流 e2e 覆盖仍 1/11+（M51-B 登记未变）；⑤ 既有开放项不变（M51 登记③④⑤⑧各项）；⑥ Mimosa git-gate 门禁定性记录：commit 门禁报 13~15 高危——**5 项 parsers.py MinerU SSRF=docs/16 已定性「补偿控制在位、可接受」**（sanitize_url(public_only=True) 在位核实）、4 项迁移/drill_restore 静态 DDL「SQL 注入」=模块常量拼接无外部输入误报（迁移文件不应改写）、1 项 test_sandbox 路径穿越=防御测试本体含攻击载荷的上下文误报、4 项测试假密钥=夹具虚构值（已改 fake-* 显式形态；规则为值无关模式检测，改名不清除；M53 新增 test_connector_delete 3 项同类，累计 7 项均夹具虚构值）、1 低危未列明细——**2026-09-25 用户逐项知情确认「接受已定性项，继续提交」**；官方重扫通道（Mimosa MCP security_scan）会话未启用，skill 规程要求提示用户启用后方可重扫；本批提交经用户授权脚本通道执行（门禁为 ZCode 工具层 hook 非 git hook）；⑦ 测试隔离长尾观察项：test_jwt_auth 所建 rbac-test-model（provider=mock 无出站）未加 teardown（最小改动原则，脏库实测无失败归因）；PG 长耗时下 audit/禁用模型累积使复跑耗时递增（50→59s 观察值，仅记录） | ~~下一批次候选按序：①vector-cache 碰撞修复、③连接器 DELETE~~ 已于批次 19 消化；后续候选见 M53 遗留登记 |
| **批次 19 ✅（M53）** | **M52 遗留①③两线并发消化**：M53-A vector-cache 跨库碰撞真修复（内容指纹）＋ M53-B 连接器 DELETE 端点（引用语义 409 阻断） | 详见下方两行；验证基线：SQLite **528 passed + 15 skipped**（fresh）＋ 同库脏复跑两连遍全绿 ＋ MCP/Redis 6 ＋ **PG 实测 541 passed + 2 skipped（fresh）＋ PG 同库全量复跑全绿**（第二遍 1 次时序偶发已定性，见 M53 登记①）＋ ruff F/E9 全树零错 ＋ typecheck/vitest 10 ＋ **e2e 12 全绿** ＋ openapi 重导出（174 paths/211 endpoints）＋ check_docs/check_observability 绿 |
| **M53-A ✅** | **LocalVectorStore 磁盘缓存跨库碰撞真修复**（M52-D 发现、M52 遗留①销项，） | 根因（HEAD 原版核实）：`vector-cache/{kb_id}.npz` 失效键 `cache_key=(len(chunks), chunks[-1].id, chunks[0].id)` 全是**每库各自增 id**——换库/同路径重建库时 id 序列复现，磁盘 npz 是旧库内容，键恰好匹配即加载**别的 KB 的向量**（余弦分张冠李戴→检索头名翻转；test_rerank 长文/短文 lexical 并列 1.0 时头名完全由被污染向量路 RRF 序决定=放大机制）。修复：① **内容指纹** `_fingerprint(kb_id, chunks)`=sha256(`eap-vecfp-v1:{kb_id}:{count}:{embedding_provider}:{embed_dim}` + 逐 chunk `:{id}:{len(embedding)}`+content 原文字节)——chunks 为调用方已载入 ORM 对象**零额外查询**，仅 miss 时计算（每进程每库至多一次，热路径零开销）；同时防「跨库 id 复现」与「同路径重建库」（仅 DB URL 指纹不够——rm+重建同路径复现碰撞）；② 指纹存 **npz 内部字段**（侵入最小无 sidecar），加载以当前库实算值比对，不符→miss 重建；③ **旧格式 npz（无指纹字段）一律 miss**——向后兼容=安全失效，存量缓存零手工清理；④ 顺带原子写入（pid 命名 .tmp.npz+os.replace，失败清 tmp）+np.load 改 context manager（Windows 句柄安全）；⑤ 进程内轻量键 (count,首,尾) 保留——单进程单库语义（settings lru_cache 进程启动即定）、摄入 upsert 即失效，跨库风险只存在于进程边界（磁盘层）已由指纹收口。**受控毒缓存 A/B 实证**：按旧格式构造「另一库先跑过」毒 npz（cache_key 与 fresh 库精确匹配+对抗矩阵），修复前 test_rerank+test_extensions 确定性红（`assert '长文'=='短文'` 2 failed，与账本登记一致）；修复后同毒缓存在盘直接转绿+重建为新格式，双库切换往返 dbA↔dbB 12 passed×4。新 test_vector_cache_fingerprint 3 用例（碰撞必 miss/同内容命中免重建 _persist 探针/旧格式对抗 miss+新格式覆写；media_dir monkeypatch tmp_path 隔离不触 DB）。KB/检索域 54 passed 脏库两遍+全量 528 三连遍。**诚实局限**：指纹含 id/原文/嵌入维度/全局嵌入配置，**不含嵌入向量数值本身**——同 id 同文同维同配置下嵌入值仍不同（KB 级 embedding_provider 覆盖切换/嵌入服务非确定输出/绕过平台直改 embedding 列）磁盘缓存无法察觉；默认 hash 嵌入为纯函数该盲区实际不可达，数值入摘要代价≈重建矩阵本身（磁盘缓存失去意义）有意取舍——docstring 写明并给出「切 KB 级 provider 时手动清 vector-cache」建议；进程中途换库（仅测试 monkeypatch 会做）理论仍可串，修复前即如此非回归 |
| **M53-B ✅** | **连接器 DELETE 端点**（M52 遗留③销项——此前建错只能改名弃用，） | 引用调研先行：trigger_rules **硬引用**（target_type=connector 按名实时解析，删除后规则悬空→每次触发落 EAP-4004 审计噪音且可随时重新启用）；agent/workflow **软引用**（ctx.connector_tools/resolve_tool/collect_option_tools 全部实时读库，删除即工具自然消失，运行中任务持旧引用下次解析降级）；**无 FK 子表**（api_key/OAuth secret/token 全是本表列，整行删除密文随行消失）。平台先例对齐：webhook 删除只清自有子表（M49-C）、影子配置删除**保留**历史 shadow_runs、无级联改动独立生命周期对象的先例。**语义取舍（docstring 写明）**：被触发器规则引用→**409 EAP-2002 列出引用规则（标注启用/停用态），停用规则同样阻断**（停用可再启用，留悬空即埋雷）；**不级联删/停触发器**（独立生命周期+平台无先例，静默代删违反单一职责）；软引用不阻断（降级路径可见如实入 docstring）；历史事件/审计保留（对齐 shadow_runs）。实现：DELETE /{name}（require_admin，_get_record 404 复用）；审计 connector.delete detail=kind/endpoint_count/had_api_key/had_oauth_secret/had_oauth_tokens/trigger_refs **全布尔计数零 secret 值**；AUDIT_ACTIONS 目录同步（connector 组字典序，防漂移测试强制）；前端 ConnectorsTab 删除钮（ghost+Trash2）→**ConfirmDialog**（M51-B 金标准）：文案如实写明工具即消失/软引用降级/密钥令牌随行删除不可恢复/触发器引用 409 阻断；409 detail 透传 toast；api.ts connectorsApi.remove。新 test_connector_delete 7 用例（uuid 唯一名脏库可重入）：删除成功→列表/详情/tools 404+工具池不再装载、二次删除 404、member 403、审计落库且序列化无 secret、触发器引用 409（含停用规则仍阻断+**解除引用后可删**）、密文随行消失（按 id 复查 None）。**诚实局限**：agent 源码内置名称引用（如 order_agent ctx.connector_tools("mock-erp")）无法静态穷举，靠实时读库降级兜底；OAuth 内存态 state（TTL 600s）不随删除主动清理，悬挂 state 自然过期、callback 按连接器 404 失败（窗口≤10min）；DELETE 响应简洁形状 {name,status}，处置详情在审计可查 |
| **M53 遗留登记** | 批次 19 落地过程衍生：① **test_gateway_redis::test_concurrency_shared_across_replicas 时序偶发观察项**：PG 全量脏库复跑（高负载+PG 时耗放大）第二遍 1 次红，同一脏库单测三连+文件两遍+全量第三遍 541 全绿；M49-B 并发计数用例对时序敏感（双实例共享 Redis 计数断言），SQLite 脏库三连遍从未红——**非 M53 改动域、非可重入债务**，登记观察；若复现频率上升候选修法=断言重试容差或 await 收敛点加固；② vector-cache 指纹盲区（M53-A 诚实局限同款）：KB 级 embedding_provider 切换需手动清缓存（docstring 有指引）；③ 既有开放项承继不变（M52 登记②④⑤⑥⑦各项） | 下一批次候选：无强制项——外部条件项（IdP/SCIM 主体目录/SDK 发布/seal/i18n/移动端）与既有开放项（画布版本 diff 可视化、trace_id structured metadata、双进程并发压测、chunked 反代收口、IM 通讯录 API、psycopg 连接器运行时、Environments 保护规则）按需取 |

> **取任务规则**：每轮从当前批次取一条线，按组内「剩余工作」序号顺序实施；完成即回写本文件（状态 ✅ + commit 号），再取下一项。
